// AUTO-GENERATED from the former blog.ts — full article bodies (EN/FR/AR markdown).
export interface BlogBody {
  en: string;
  fr: string;
  ar: string;
}

export const blogBodies: Record<string, BlogBody> = {
  "lightgbm-fraud-detection-auc-0964": {"en":"## The Problem\n\nThe IEEE-CIS Fraud Detection challenge presents 590,540 training transactions with 433 features and only 3.5% fraud rate.\n\n## Key Feature Engineering\n\n- **Time-based features**: Hour of day, day of week, temporal drift\n- **Card aggregations**: Mean/std/count of TransactionAmt per card1/card2\n- **Email domain features**: same_email_domain flag, domain-level fraud rates\n- **M-column boolean counts**: T/F/missing across M1-M9\n\n## Model Pipeline\n\n| Model | OOF AUC |\n|-------|---------|\n| LightGBM | **0.9648** |\n| XGBoost | 0.9631 |\n| CatBoost | 0.9529 |\n\n## Key Insights\n\n1. Don't drop V-columns — they carry Vesta's proprietary fraud signals\n2. Time-based CV is more realistic than StratifiedKFold\n3. Card-level aggregations are the highest-impact feature group\n4. LightGBM's native missing-value handling gives it the edge","fr":"## Le problème\n\nLe défi IEEE-CIS Fraud Detection présente 590 540 transactions d'entraînement avec 433 caractéristiques et un taux de fraude de seulement 3,5 %.\n\n## Feature engineering clé\n\n- **Caractéristiques temporelles** : heure de la journée, jour de la semaine, dérive temporelle\n- **Agrégations par carte** : moyenne/écart-type/comptage de TransactionAmt par card1/card2\n- **Caractéristiques de domaine e-mail** : indicateur same_email_domain, taux de fraude par domaine\n- **Comptages booléens des colonnes M** : T/F/manquant sur M1-M9\n\n## Pipeline de modèles\n\n| Modèle | AUC OOF |\n|-------|---------|\n| LightGBM | **0.9648** |\n| XGBoost | 0.9631 |\n| CatBoost | 0.9529 |\n\n## Points clés\n\n1. Ne supprimez pas les colonnes V — elles portent les signaux de fraude propriétaires de Vesta\n2. La validation croisée temporelle est plus réaliste que StratifiedKFold\n3. Les agrégations au niveau carte sont le groupe de caractéristiques le plus impactant\n4. La gestion native des valeurs manquantes de LightGBM lui donne l'avantage","ar":"## المشكلة\n\nيقدّم تحدي IEEE-CIS Fraud Detection عدد 590,540 معاملة تدريب مع 433 ميزة ونسبة احتيال لا تتجاوز 3.5%.\n\n## أبرز هندسة الميزات\n\n- **ميزات زمنية**: ساعة اليوم، يوم الأسبوع، الانحراف الزمني\n- **تجميعات البطاقة**: المتوسط/الانحراف المعياري/العدد لـ TransactionAmt لكل card1/card2\n- **ميزات نطاق البريد**: علامة same_email_domain، ومعدلات الاحتيال حسب النطاق\n- **عدّ القيم المنطقية لأعمدة M**: T/F/مفقود عبر M1-M9\n\n## خط أنابيب النماذج\n\n| النموذج | OOF AUC |\n|-------|---------|\n| LightGBM | **0.9648** |\n| XGBoost | 0.9631 |\n| CatBoost | 0.9529 |\n\n## رؤى رئيسية\n\n1. لا تحذف أعمدة V — فهي تحمل إشارات الاحتيال الخاصة بشركة Vesta\n2. التحقق المتقاطع الزمني أكثر واقعية من StratifiedKFold\n3. التجميعات على مستوى البطاقة هي أكثر مجموعات الميزات تأثيراً\n4. المعالجة الأصلية للقيم المفقودة في LightGBM تمنحه الأفضلية"},
  "xgboost-vs-lightgbm-deep-comparison": {"en":"## TL;DR\n\n- **Use LightGBM** when speed matters most and your dataset is large (>100K rows)\n- **Use XGBoost** when you need reproducibility and battle-tested stability\n- **Use CatBoost** when you have many high-cardinality categoricals\n\n## Training Speed Benchmark\n\nOn 500K rows, 200 features, 1000 trees:\n\n| Model | Time | RAM |\n|-------|------|-----|\n| LightGBM | 45s | 2.1GB |\n| XGBoost | 210s | 4.8GB |\n| CatBoost | 130s | 3.2GB |\n\n## When XGBoost Wins\n\n1. Exact split finding on small datasets\n2. Better with sparse data (text features as TF-IDF)\n3. More stable across random seeds\n\n## When LightGBM Wins\n\n1. Large datasets (leaf-wise growth is faster)\n2. Native categorical handling\n3. DART for better regularization","fr":"## En bref\n\n- **Utilisez LightGBM** quand la vitesse prime et que le jeu de données est grand (>100K lignes)\n- **Utilisez XGBoost** quand vous avez besoin de reproductibilité et d'une stabilité éprouvée\n- **Utilisez CatBoost** quand vous avez de nombreuses variables catégorielles à forte cardinalité\n\n## Benchmark de vitesse d'entraînement\n\nSur 500K lignes, 200 caractéristiques, 1000 arbres :\n\n| Modèle | Temps | RAM |\n|-------|------|-----|\n| LightGBM | 45s | 2.1GB |\n| XGBoost | 210s | 4.8GB |\n| CatBoost | 130s | 3.2GB |\n\n## Quand XGBoost l'emporte\n\n1. Recherche exacte des splits sur petits jeux de données\n2. Meilleur avec des données creuses (caractéristiques texte en TF-IDF)\n3. Plus stable d'une graine aléatoire à l'autre\n\n## Quand LightGBM l'emporte\n\n1. Grands jeux de données (la croissance leaf-wise est plus rapide)\n2. Gestion native des variables catégorielles\n3. DART pour une meilleure régularisation","ar":"## باختصار\n\n- **استخدم LightGBM** عندما تكون السرعة هي الأهم ومجموعة البيانات كبيرة (>100K صف)\n- **استخدم XGBoost** عندما تحتاج إلى قابلية إعادة الإنتاج واستقرار مُجرَّب\n- **استخدم CatBoost** عندما يكون لديك العديد من المتغيرات الفئوية عالية التعددية\n\n## قياس سرعة التدريب\n\nعلى 500K صف، 200 ميزة، 1000 شجرة:\n\n| النموذج | الزمن | الذاكرة |\n|-------|------|-----|\n| LightGBM | 45s | 2.1GB |\n| XGBoost | 210s | 4.8GB |\n| CatBoost | 130s | 3.2GB |\n\n## متى يتفوق XGBoost\n\n1. إيجاد التقسيمات الدقيق على مجموعات البيانات الصغيرة\n2. أفضل مع البيانات المتفرقة (ميزات النص كـ TF-IDF)\n3. أكثر استقراراً عبر البذور العشوائية\n\n## متى يتفوق LightGBM\n\n1. مجموعات البيانات الكبيرة (النمو leaf-wise أسرع)\n2. المعالجة الأصلية للمتغيرات الفئوية\n3. DART لتنظيم أفضل"},
  "feature-engineering-tabular-ml": {"en":"## The 15 Techniques\n\n### 1. Target Encoding (with smoothing)\n```python\nsmoothed = (count * category_mean + global_mean * smoothing) / (count + smoothing)\n```\n\n### 2. Frequency Encoding\nReplace categorical value with its frequency in the training set.\n\n### 3. Lag Features\nFor time series: lag-1, lag-7, lag-30 values of the target.\n\n### 4. Rolling Statistics\nMean, std, min, max over rolling windows.\n\n### 5. Interaction Terms\nMultiply or divide two numerical features that have domain meaning.\n\n### 6. Date Parts\nExtract year, month, day, hour, weekday, quarter, is_weekend.\n\n### 7. Rank Features\nRank within group — useful for normalizing scale.\n\n### 8. Aggregation Features\nGroup by entity (user, card, store) and compute statistics.\n\n### 9-15. More in the full post...","fr":"## Les 15 techniques\n\n### 1. Target encoding (avec lissage)\n```python\nsmoothed = (count * category_mean + global_mean * smoothing) / (count + smoothing)\n```\n\n### 2. Frequency encoding\nRemplacez la valeur catégorielle par sa fréquence dans l'ensemble d'entraînement.\n\n### 3. Caractéristiques de décalage (lag)\nPour les séries temporelles : valeurs lag-1, lag-7, lag-30 de la cible.\n\n### 4. Statistiques glissantes\nMoyenne, écart-type, min, max sur des fenêtres glissantes.\n\n### 5. Termes d'interaction\nMultipliez ou divisez deux caractéristiques numériques ayant un sens métier.\n\n### 6. Composants de date\nExtrayez année, mois, jour, heure, jour de semaine, trimestre, is_weekend.\n\n### 7. Caractéristiques de rang\nClassement au sein d'un groupe — utile pour normaliser l'échelle.\n\n### 8. Caractéristiques d'agrégation\nGroupez par entité (utilisateur, carte, magasin) et calculez des statistiques.\n\n### 9-15. La suite dans l'article complet...","ar":"## التقنيات الـ15\n\n### 1. ترميز الهدف (مع التنعيم)\n```python\nsmoothed = (count * category_mean + global_mean * smoothing) / (count + smoothing)\n```\n\n### 2. ترميز التكرار\nاستبدل القيمة الفئوية بتكرارها في مجموعة التدريب.\n\n### 3. ميزات الإزاحة (lag)\nللسلاسل الزمنية: قيم lag-1 وlag-7 وlag-30 للهدف.\n\n### 4. الإحصاءات المتحركة\nالمتوسط والانحراف المعياري والحد الأدنى والأقصى عبر نوافذ متحركة.\n\n### 5. حدود التفاعل\nاضرب أو اقسم ميزتين رقميتين لهما معنى في المجال.\n\n### 6. مكوّنات التاريخ\nاستخرج السنة والشهر واليوم والساعة ويوم الأسبوع والربع وis_weekend.\n\n### 7. ميزات الرتبة\nالترتيب داخل المجموعة — مفيد لتطبيع المقياس.\n\n### 8. ميزات التجميع\nجمّع حسب الكيان (مستخدم، بطاقة، متجر) واحسب الإحصاءات.\n\n### 9-15. المزيد في المقال الكامل..."},
  "catboost-categorical-features-guide": {"en":"## The Data Leakage Problem\n\nNaive target encoding leaks future information: when you encode a category with its mean target, you use information from the same row to encode itself.\n\n## CatBoost's Solution: Ordered Target Encoding\n\nCatBoost processes rows in a random order. When encoding row i, it only uses statistics computed from rows 0..i-1. This prevents leakage by construction.\n\n## Implementation\n\n```python\nfrom catboost import CatBoostClassifier\n\nmodel = CatBoostClassifier(\n    cat_features=['city', 'device_type', 'email_domain'],\n    iterations=1000,\n    learning_rate=0.05,\n    depth=6,\n)\nmodel.fit(X_train, y_train)\n```\n\nNo manual encoding needed — pass raw strings directly.","fr":"## Le problème de fuite de données\n\nLe target encoding naïf laisse fuiter l'information future : en encodant une catégorie par la moyenne de sa cible, vous utilisez l'information de la même ligne pour s'encoder elle-même.\n\n## La solution de CatBoost : ordered target encoding\n\nCatBoost traite les lignes dans un ordre aléatoire. Pour encoder la ligne i, il n'utilise que les statistiques calculées sur les lignes 0..i-1. Cela empêche la fuite par construction.\n\n## Implémentation\n\n```python\nfrom catboost import CatBoostClassifier\n\nmodel = CatBoostClassifier(\n    cat_features=['city', 'device_type', 'email_domain'],\n    iterations=1000,\n    learning_rate=0.05,\n    depth=6,\n)\nmodel.fit(X_train, y_train)\n```\n\nAucun encodage manuel nécessaire — passez directement les chaînes brutes.","ar":"## مشكلة تسرب البيانات\n\nيُسرّب ترميز الهدف الساذج معلومات مستقبلية: عند ترميز فئة بمتوسط هدفها، فإنك تستخدم معلومات من الصف نفسه لترميزه.\n\n## حل CatBoost: الترميز المرتب للهدف\n\nيعالج CatBoost الصفوف بترتيب عشوائي. وعند ترميز الصف i، يستخدم فقط الإحصاءات المحسوبة من الصفوف 0..i-1. وهذا يمنع التسرب بحكم التصميم.\n\n## التنفيذ\n\n```python\nfrom catboost import CatBoostClassifier\n\nmodel = CatBoostClassifier(\n    cat_features=['city', 'device_type', 'email_domain'],\n    iterations=1000,\n    learning_rate=0.05,\n    depth=6,\n)\nmodel.fit(X_train, y_train)\n```\n\nلا حاجة لأي ترميز يدوي — مرّر السلاسل النصية الخام مباشرة."},
  "imbalanced-classification-strategies": {"en":"## The Imbalance Spectrum\n\n- **Mild (1:10)**: Class weights usually sufficient\n- **Moderate (1:100)**: SMOTE + class weights\n- **Severe (1:1000+)**: Anomaly detection framing, cost-sensitive learning\n\n## Technique Ranking (from my experience)\n\n1. **Threshold tuning** — Always do this. Default 0.5 is almost never optimal.\n2. **Class weights** — Easy, no data distortion, works 90% of the time.\n3. **SMOTE** — Helps on small datasets. Can hurt on large ones.\n4. **Oversampling minority** — Simple, often underrated.\n5. **Undersampling majority** — Loses information. Use carefully.\n\n## The Right Metric\n\nNever use accuracy on imbalanced data. Use:\n- F1 / F2 score\n- PR-AUC (better than ROC-AUC for severe imbalance)\n- Business-relevant cost matrix","fr":"## Le spectre du déséquilibre\n\n- **Léger (1:10)** : les poids de classe suffisent généralement\n- **Modéré (1:100)** : SMOTE + poids de classe\n- **Sévère (1:1000+)** : cadrage en détection d'anomalies, apprentissage sensible au coût\n\n## Classement des techniques (d'après mon expérience)\n\n1. **Ajustement du seuil** — À faire systématiquement. 0,5 par défaut n'est presque jamais optimal.\n2. **Poids de classe** — Simple, sans distorsion des données, fonctionne 90 % du temps.\n3. **SMOTE** — Utile sur les petits jeux de données. Peut nuire sur les grands.\n4. **Suréchantillonnage de la minorité** — Simple, souvent sous-estimé.\n5. **Sous-échantillonnage de la majorité** — Perte d'information. À utiliser avec prudence.\n\n## La bonne métrique\n\nN'utilisez jamais l'accuracy sur des données déséquilibrées. Utilisez :\n- Score F1 / F2\n- PR-AUC (meilleure que ROC-AUC en cas de fort déséquilibre)\n- Matrice de coûts pertinente pour le métier","ar":"## طيف اختلال التوازن\n\n- **خفيف (1:10)**: أوزان الفئات كافية عادةً\n- **متوسط (1:100)**: SMOTE + أوزان الفئات\n- **شديد (1:1000+)**: التأطير ككشف للشذوذ، والتعلّم الحسّاس للتكلفة\n\n## ترتيب التقنيات (من خبرتي)\n\n1. **ضبط العتبة** — افعل ذلك دائماً. القيمة الافتراضية 0.5 نادراً ما تكون مثالية.\n2. **أوزان الفئات** — سهلة، دون تشويه البيانات، وتنجح في 90% من الحالات.\n3. **SMOTE** — يفيد على المجموعات الصغيرة. وقد يضر بالكبيرة.\n4. **زيادة عينات الأقلية** — بسيط وغالباً ما يُبخس قدره.\n5. **تقليل عينات الأغلبية** — يفقد المعلومات. استخدمه بحذر.\n\n## المقياس الصحيح\n\nلا تستخدم الدقة (accuracy) أبداً على البيانات غير المتوازنة. استخدم:\n- درجة F1 / F2\n- PR-AUC (أفضل من ROC-AUC عند الاختلال الشديد)\n- مصفوفة تكلفة ملائمة للأعمال"},
  "optuna-hyperparameter-tuning-guide": {"en":"## Why Optuna Over GridSearch\n\n- **Bayesian optimization** learns from past trials\n- **Pruning** stops unpromising trials early (saves 60-80% compute)\n- **Persistent storage** resumes studies across sessions\n\n## Minimal Example\n\n```python\nimport optuna\n\ndef objective(trial):\n    params = {\n        'n_estimators': trial.suggest_int('n_estimators', 100, 2000),\n        'learning_rate': trial.suggest_float('lr', 0.001, 0.3, log=True),\n        'max_depth': trial.suggest_int('max_depth', 3, 10),\n    }\n    model = LGBMClassifier(**params)\n    return cross_val_score(model, X, y, cv=5, scoring='roc_auc').mean()\n\nstudy = optuna.create_study(direction='maximize')\nstudy.optimize(objective, n_trials=100)\n```\n\n## Pruning with Callbacks\n\n```python\nfrom optuna.integration import LightGBMPruningCallback\ncallback = LightGBMPruningCallback(trial, 'auc')\nmodel.fit(X_train, y_train, callbacks=[callback])\n```","fr":"## Pourquoi Optuna plutôt que GridSearch\n\n- **L'optimisation bayésienne** apprend des essais passés\n- **L'élagage (pruning)** arrête tôt les essais peu prometteurs (économise 60-80 % de calcul)\n- **Le stockage persistant** reprend les études entre les sessions\n\n## Exemple minimal\n\n```python\nimport optuna\n\ndef objective(trial):\n    params = {\n        'n_estimators': trial.suggest_int('n_estimators', 100, 2000),\n        'learning_rate': trial.suggest_float('lr', 0.001, 0.3, log=True),\n        'max_depth': trial.suggest_int('max_depth', 3, 10),\n    }\n    model = LGBMClassifier(**params)\n    return cross_val_score(model, X, y, cv=5, scoring='roc_auc').mean()\n\nstudy = optuna.create_study(direction='maximize')\nstudy.optimize(objective, n_trials=100)\n```\n\n## Élagage avec callbacks\n\n```python\nfrom optuna.integration import LightGBMPruningCallback\ncallback = LightGBMPruningCallback(trial, 'auc')\nmodel.fit(X_train, y_train, callbacks=[callback])\n```","ar":"## لماذا Optuna بدلاً من GridSearch\n\n- **التحسين البايزي** يتعلّم من المحاولات السابقة\n- **التقليم (pruning)** يوقف المحاولات غير الواعدة مبكراً (يوفّر 60-80% من الحوسبة)\n- **التخزين الدائم** يستأنف الدراسات عبر الجلسات\n\n## مثال مبسّط\n\n```python\nimport optuna\n\ndef objective(trial):\n    params = {\n        'n_estimators': trial.suggest_int('n_estimators', 100, 2000),\n        'learning_rate': trial.suggest_float('lr', 0.001, 0.3, log=True),\n        'max_depth': trial.suggest_int('max_depth', 3, 10),\n    }\n    model = LGBMClassifier(**params)\n    return cross_val_score(model, X, y, cv=5, scoring='roc_auc').mean()\n\nstudy = optuna.create_study(direction='maximize')\nstudy.optimize(objective, n_trials=100)\n```\n\n## التقليم باستخدام callbacks\n\n```python\nfrom optuna.integration import LightGBMPruningCallback\ncallback = LightGBMPruningCallback(trial, 'auc')\nmodel.fit(X_train, y_train, callbacks=[callback])\n```"},
  "shap-model-explainability-production": {"en":"## Why SHAP?\n\nSHAP (SHapley Additive exPlanations) gives theoretically grounded feature importance — not the biased impurity importance from tree models.\n\n## Three Key Plots\n\n### 1. Beeswarm (Global)\n```python\nimport shap\nexplainer = shap.TreeExplainer(model)\nshap_values = explainer(X_test)\nshap.plots.beeswarm(shap_values)\n```\n\n### 2. Waterfall (Local — one prediction)\n```python\nshap.plots.waterfall(shap_values[0])\n```\n\n### 3. Dependence Plot\n```python\nshap.plots.scatter(shap_values[:, 'TransactionAmt'])\n```\n\n## Stakeholder-Friendly Framing\n\n\"This transaction was flagged as fraud because the amount ($3,200) is 4x higher than typical for this card, and the email domain changed in the last 24h.\"","fr":"## Pourquoi SHAP ?\n\nSHAP (SHapley Additive exPlanations) fournit une importance des caractéristiques fondée théoriquement — pas l'importance par impureté, biaisée, des modèles d'arbres.\n\n## Trois graphiques clés\n\n### 1. Beeswarm (global)\n```python\nimport shap\nexplainer = shap.TreeExplainer(model)\nshap_values = explainer(X_test)\nshap.plots.beeswarm(shap_values)\n```\n\n### 2. Waterfall (local — une prédiction)\n```python\nshap.plots.waterfall(shap_values[0])\n```\n\n### 3. Graphique de dépendance\n```python\nshap.plots.scatter(shap_values[:, 'TransactionAmt'])\n```\n\n## Une formulation adaptée aux parties prenantes\n\n« Cette transaction a été signalée comme frauduleuse car le montant (3 200 $) est 4x plus élevé que d'habitude pour cette carte, et le domaine e-mail a changé au cours des 24 dernières heures. »","ar":"## لماذا SHAP؟\n\nيوفّر SHAP (SHapley Additive exPlanations) أهمية للميزات مؤسَّسة نظرياً — وليست أهمية الشوائب المتحيّزة في نماذج الأشجار.\n\n## ثلاثة رسوم رئيسية\n\n### 1. Beeswarm (عام)\n```python\nimport shap\nexplainer = shap.TreeExplainer(model)\nshap_values = explainer(X_test)\nshap.plots.beeswarm(shap_values)\n```\n\n### 2. Waterfall (محلي — تنبؤ واحد)\n```python\nshap.plots.waterfall(shap_values[0])\n```\n\n### 3. رسم الاعتمادية\n```python\nshap.plots.scatter(shap_values[:, 'TransactionAmt'])\n```\n\n## صياغة مناسبة لأصحاب المصلحة\n\n«تم وضع علامة احتيال على هذه المعاملة لأن المبلغ (3,200 دولار) أعلى بأربع مرات من المعتاد لهذه البطاقة، ولأن نطاق البريد الإلكتروني تغيّر خلال آخر 24 ساعة.»"},
  "cross-validation-strategies-guide": {"en":"## Decision Tree\n\n1. **Time series data?** → TimeSeriesSplit (never shuffle time!)\n2. **Groups that must stay together?** → GroupKFold\n3. **Imbalanced classes?** → StratifiedKFold\n4. **Default tabular** → StratifiedKFold with 5 folds\n\n## TimeSeriesSplit Example\n\n```python\nfrom sklearn.model_selection import TimeSeriesSplit\ntscv = TimeSeriesSplit(n_splits=5)\nfor train_idx, val_idx in tscv.split(X):\n    X_train, X_val = X.iloc[train_idx], X.iloc[val_idx]\n```\n\n## GroupKFold: Preventing Leakage\n\nIf you have user IDs, patient IDs, or store IDs — always use GroupKFold so the same entity never appears in both train and validation.","fr":"## Arbre de décision\n\n1. **Données de séries temporelles ?** → TimeSeriesSplit (ne mélangez jamais le temps !)\n2. **Des groupes qui doivent rester ensemble ?** → GroupKFold\n3. **Classes déséquilibrées ?** → StratifiedKFold\n4. **Tabulaire par défaut** → StratifiedKFold à 5 plis\n\n## Exemple TimeSeriesSplit\n\n```python\nfrom sklearn.model_selection import TimeSeriesSplit\ntscv = TimeSeriesSplit(n_splits=5)\nfor train_idx, val_idx in tscv.split(X):\n    X_train, X_val = X.iloc[train_idx], X.iloc[val_idx]\n```\n\n## GroupKFold : éviter la fuite\n\nSi vous avez des IDs utilisateur, patient ou magasin — utilisez toujours GroupKFold pour qu'une même entité n'apparaisse jamais à la fois en entraînement et en validation.","ar":"## شجرة القرار\n\n1. **بيانات سلاسل زمنية؟** ← TimeSeriesSplit (لا تخلط الزمن أبداً!)\n2. **مجموعات يجب أن تبقى معاً؟** ← GroupKFold\n3. **فئات غير متوازنة؟** ← StratifiedKFold\n4. **جدولي افتراضي** ← StratifiedKFold بخمس طيات\n\n## مثال TimeSeriesSplit\n\n```python\nfrom sklearn.model_selection import TimeSeriesSplit\ntscv = TimeSeriesSplit(n_splits=5)\nfor train_idx, val_idx in tscv.split(X):\n    X_train, X_val = X.iloc[train_idx], X.iloc[val_idx]\n```\n\n## GroupKFold: منع التسرب\n\nإذا كان لديك معرّفات مستخدمين أو مرضى أو متاجر — استخدم GroupKFold دائماً حتى لا يظهر الكيان نفسه في التدريب والتحقق معاً."},
  "u-net-medical-segmentation-dice-0796": {"en":"## Architecture: U-Net with EfficientNet Encoder\n\n```python\nimport segmentation_models_pytorch as smp\n\nmodel = smp.Unet(\n    encoder_name='efficientnet-b4',\n    encoder_weights='imagenet',\n    in_channels=3,\n    classes=1,\n)\n```\n\n## Loss Function: Combo Loss\n\nDice + BCE combination outperformed either alone:\n```python\nloss = 0.5 * bce_loss + 0.5 * (1 - dice_score)\n```\n\n## Augmentation Stack (Albumentations)\n\n- RandomResizedCrop, HorizontalFlip, VerticalFlip\n- ElasticTransform, GridDistortion\n- CLAHE, RandomBrightness\n- CoarseDropout (Cutout)\n\n## Post-processing\n\nTest-time augmentation (TTA) with 8 flips/rotations added +0.015 Dice.","fr":"## Architecture : U-Net avec encodeur EfficientNet\n\n```python\nimport segmentation_models_pytorch as smp\n\nmodel = smp.Unet(\n    encoder_name='efficientnet-b4',\n    encoder_weights='imagenet',\n    in_channels=3,\n    classes=1,\n)\n```\n\n## Fonction de perte : Combo Loss\n\nLa combinaison Dice + BCE a surpassé chacune prise isolément :\n```python\nloss = 0.5 * bce_loss + 0.5 * (1 - dice_score)\n```\n\n## Pile d'augmentation (Albumentations)\n\n- RandomResizedCrop, HorizontalFlip, VerticalFlip\n- ElasticTransform, GridDistortion\n- CLAHE, RandomBrightness\n- CoarseDropout (Cutout)\n\n## Post-traitement\n\nL'augmentation au moment du test (TTA) avec 8 retournements/rotations a ajouté +0,015 Dice.","ar":"## المعمارية: U-Net مع مُرمِّز EfficientNet\n\n```python\nimport segmentation_models_pytorch as smp\n\nmodel = smp.Unet(\n    encoder_name='efficientnet-b4',\n    encoder_weights='imagenet',\n    in_channels=3,\n    classes=1,\n)\n```\n\n## دالة الخسارة: Combo Loss\n\nتفوّق الجمع بين Dice وBCE على كلٍّ منهما بمفرده:\n```python\nloss = 0.5 * bce_loss + 0.5 * (1 - dice_score)\n```\n\n## حزمة التوسيع (Albumentations)\n\n- RandomResizedCrop، HorizontalFlip، VerticalFlip\n- ElasticTransform، GridDistortion\n- CLAHE، RandomBrightness\n- CoarseDropout (Cutout)\n\n## المعالجة اللاحقة\n\nأضاف التوسيع وقت الاختبار (TTA) بثمانية انعكاسات/تدويرات +0.015 إلى Dice."},
  "yolov8-custom-object-detection": {"en":"## Dataset Preparation\n\n1. Annotate with Roboflow or LabelImg\n2. Export in YOLOv8 format\n3. Data split: 80/10/10 train/val/test\n\n## Training\n\n```python\nfrom ultralytics import YOLO\n\nmodel = YOLO('yolov8n.pt')  # nano for fast prototyping\nresults = model.train(\n    data='dataset.yaml',\n    epochs=100,\n    imgsz=640,\n    batch=16,\n    augment=True,\n)\n```\n\n## ONNX Export for Production\n\n```python\nmodel.export(format='onnx', dynamic=True, simplify=True)\n```\n\n## FastAPI Endpoint\n\n```python\n@app.post('/detect')\nasync def detect(file: UploadFile):\n    img = Image.open(file.file)\n    results = model(img)\n    return {'detections': results[0].boxes.json()}\n```","fr":"## Préparation du jeu de données\n\n1. Annotez avec Roboflow ou LabelImg\n2. Exportez au format YOLOv8\n3. Découpage : 80/10/10 entraînement/validation/test\n\n## Entraînement\n\n```python\nfrom ultralytics import YOLO\n\nmodel = YOLO('yolov8n.pt')  # nano for fast prototyping\nresults = model.train(\n    data='dataset.yaml',\n    epochs=100,\n    imgsz=640,\n    batch=16,\n    augment=True,\n)\n```\n\n## Export ONNX pour la production\n\n```python\nmodel.export(format='onnx', dynamic=True, simplify=True)\n```\n\n## Endpoint FastAPI\n\n```python\n@app.post('/detect')\nasync def detect(file: UploadFile):\n    img = Image.open(file.file)\n    results = model(img)\n    return {'detections': results[0].boxes.json()}\n```","ar":"## تجهيز مجموعة البيانات\n\n1. علّق البيانات باستخدام Roboflow أو LabelImg\n2. صدّر بصيغة YOLOv8\n3. التقسيم: 80/10/10 تدريب/تحقق/اختبار\n\n## التدريب\n\n```python\nfrom ultralytics import YOLO\n\nmodel = YOLO('yolov8n.pt')  # nano for fast prototyping\nresults = model.train(\n    data='dataset.yaml',\n    epochs=100,\n    imgsz=640,\n    batch=16,\n    augment=True,\n)\n```\n\n## تصدير ONNX للإنتاج\n\n```python\nmodel.export(format='onnx', dynamic=True, simplify=True)\n```\n\n## نقطة نهاية FastAPI\n\n```python\n@app.post('/detect')\nasync def detect(file: UploadFile):\n    img = Image.open(file.file)\n    results = model(img)\n    return {'detections': results[0].boxes.json()}\n```"},
  "stable-diffusion-controlnet-workflow": {"en":"## Architecture Overview\n\nRunpod serverless endpoint + Redis queue + FastAPI router\n\n## ControlNet Pipeline\n\n```python\nfrom diffusers import StableDiffusionControlNetPipeline, ControlNetModel\n\ncontrolnet = ControlNetModel.from_pretrained('lllyasviel/control_v11p_sd15_canny')\npipe = StableDiffusionControlNetPipeline.from_pretrained(\n    'runwayml/stable-diffusion-v1-5',\n    controlnet=controlnet,\n    torch_dtype=torch.float16,\n).to('cuda')\n```\n\n## Production Optimizations\n\n1. **torch.compile()** — 35% speedup on A100\n2. **xformers attention** — 40% VRAM reduction\n3. **Batched inference** — process 4 images simultaneously\n4. **NSFW filter** — required for public APIs\n\n## Prompt Engineering for Consistency\n\nAlways include quality tokens: `masterpiece, best quality, 8k, detailed`","fr":"## Vue d'ensemble de l'architecture\n\nEndpoint serverless Runpod + file Redis + routeur FastAPI\n\n## Pipeline ControlNet\n\n```python\nfrom diffusers import StableDiffusionControlNetPipeline, ControlNetModel\n\ncontrolnet = ControlNetModel.from_pretrained('lllyasviel/control_v11p_sd15_canny')\npipe = StableDiffusionControlNetPipeline.from_pretrained(\n    'runwayml/stable-diffusion-v1-5',\n    controlnet=controlnet,\n    torch_dtype=torch.float16,\n).to('cuda')\n```\n\n## Optimisations de production\n\n1. **torch.compile()** — 35 % d'accélération sur A100\n2. **xformers attention** — 40 % de VRAM en moins\n3. **Inférence par lots** — traite 4 images simultanément\n4. **Filtre NSFW** — requis pour les API publiques\n\n## Prompt engineering pour la cohérence\n\nIncluez toujours des tokens de qualité : `masterpiece, best quality, 8k, detailed`","ar":"## نظرة عامة على المعمارية\n\nنقطة نهاية Runpod بدون خادم + طابور Redis + موجِّه FastAPI\n\n## خط أنابيب ControlNet\n\n```python\nfrom diffusers import StableDiffusionControlNetPipeline, ControlNetModel\n\ncontrolnet = ControlNetModel.from_pretrained('lllyasviel/control_v11p_sd15_canny')\npipe = StableDiffusionControlNetPipeline.from_pretrained(\n    'runwayml/stable-diffusion-v1-5',\n    controlnet=controlnet,\n    torch_dtype=torch.float16,\n).to('cuda')\n```\n\n## تحسينات الإنتاج\n\n1. **torch.compile()** — تسريع بنسبة 35% على A100\n2. **xformers attention** — خفض ذاكرة VRAM بنسبة 40%\n3. **الاستدلال على دفعات** — معالجة 4 صور في آن واحد\n4. **مرشّح NSFW** — مطلوب لواجهات API العامة\n\n## هندسة المطالبات لتحقيق الاتساق\n\nأدرج دائماً رموز الجودة: `masterpiece, best quality, 8k, detailed`"},
  "transformer-from-scratch-pytorch": {"en":"## Multi-Head Attention\n\n```python\nclass MultiHeadAttention(nn.Module):\n    def __init__(self, d_model, n_heads):\n        super().__init__()\n        self.d_k = d_model // n_heads\n        self.n_heads = n_heads\n        self.W_q = nn.Linear(d_model, d_model)\n        self.W_k = nn.Linear(d_model, d_model)\n        self.W_v = nn.Linear(d_model, d_model)\n        self.W_o = nn.Linear(d_model, d_model)\n    \n    def forward(self, q, k, v, mask=None):\n        B = q.size(0)\n        Q = self.W_q(q).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        K = self.W_k(k).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        V = self.W_v(v).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        scores = torch.matmul(Q, K.transpose(-2,-1)) / math.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores.masked_fill(mask==0, -1e9)\n        attn = F.softmax(scores, dim=-1)\n        return self.W_o(torch.matmul(attn, V).transpose(1,2).contiguous().view(B,-1,self.n_heads*self.d_k))\n```","fr":"## Attention multi-têtes\n\n```python\nclass MultiHeadAttention(nn.Module):\n    def __init__(self, d_model, n_heads):\n        super().__init__()\n        self.d_k = d_model // n_heads\n        self.n_heads = n_heads\n        self.W_q = nn.Linear(d_model, d_model)\n        self.W_k = nn.Linear(d_model, d_model)\n        self.W_v = nn.Linear(d_model, d_model)\n        self.W_o = nn.Linear(d_model, d_model)\n    \n    def forward(self, q, k, v, mask=None):\n        B = q.size(0)\n        Q = self.W_q(q).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        K = self.W_k(k).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        V = self.W_v(v).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        scores = torch.matmul(Q, K.transpose(-2,-1)) / math.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores.masked_fill(mask==0, -1e9)\n        attn = F.softmax(scores, dim=-1)\n        return self.W_o(torch.matmul(attn, V).transpose(1,2).contiguous().view(B,-1,self.n_heads*self.d_k))\n```","ar":"## الانتباه متعدد الرؤوس\n\n```python\nclass MultiHeadAttention(nn.Module):\n    def __init__(self, d_model, n_heads):\n        super().__init__()\n        self.d_k = d_model // n_heads\n        self.n_heads = n_heads\n        self.W_q = nn.Linear(d_model, d_model)\n        self.W_k = nn.Linear(d_model, d_model)\n        self.W_v = nn.Linear(d_model, d_model)\n        self.W_o = nn.Linear(d_model, d_model)\n    \n    def forward(self, q, k, v, mask=None):\n        B = q.size(0)\n        Q = self.W_q(q).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        K = self.W_k(k).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        V = self.W_v(v).view(B, -1, self.n_heads, self.d_k).transpose(1,2)\n        scores = torch.matmul(Q, K.transpose(-2,-1)) / math.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores.masked_fill(mask==0, -1e9)\n        attn = F.softmax(scores, dim=-1)\n        return self.W_o(torch.matmul(attn, V).transpose(1,2).contiguous().view(B,-1,self.n_heads*self.d_k))\n```"},
  "training-tricks-pytorch-production": {"en":"## The 10 Tricks\n\n### 1. Mixed Precision (AMP)\n```python\nscaler = torch.cuda.amp.GradScaler()\nwith torch.autocast(device_type='cuda'):\n    loss = model(x)\nscaler.scale(loss).backward()\nscaler.step(optimizer)\nscaler.update()\n```\n**Speedup: 1.8-2.5x on modern GPUs**\n\n### 2. Gradient Checkpointing\n```python\nmodel.gradient_checkpointing_enable()\n```\n**Memory: -40%, Speed: -15%** — worth it for large models\n\n### 3. Optimal DataLoader\n```python\nDataLoader(dataset, num_workers=4, pin_memory=True, persistent_workers=True)\n```\n\n### 4. torch.compile() (PyTorch 2.0+)\n```python\nmodel = torch.compile(model)\n```\n**Speedup: 1.2-2x depending on model**\n\n### 5-10. OneCycleLR, gradient clipping, fused optimizers, CUDA streams, torch.backends.cudnn.benchmark, and memory-efficient attention.","fr":"## Les 10 astuces\n\n### 1. Précision mixte (AMP)\n```python\nscaler = torch.cuda.amp.GradScaler()\nwith torch.autocast(device_type='cuda'):\n    loss = model(x)\nscaler.scale(loss).backward()\nscaler.step(optimizer)\nscaler.update()\n```\n**Accélération : 1.8-2.5x sur GPU modernes**\n\n### 2. Gradient checkpointing\n```python\nmodel.gradient_checkpointing_enable()\n```\n**Mémoire : -40 %, Vitesse : -15 %** — rentable pour les grands modèles\n\n### 3. DataLoader optimal\n```python\nDataLoader(dataset, num_workers=4, pin_memory=True, persistent_workers=True)\n```\n\n### 4. torch.compile() (PyTorch 2.0+)\n```python\nmodel = torch.compile(model)\n```\n**Accélération : 1.2-2x selon le modèle**\n\n### 5-10. OneCycleLR, gradient clipping, optimiseurs fusionnés, CUDA streams, torch.backends.cudnn.benchmark et attention efficace en mémoire.","ar":"## الحيل العشر\n\n### 1. الدقة المختلطة (AMP)\n```python\nscaler = torch.cuda.amp.GradScaler()\nwith torch.autocast(device_type='cuda'):\n    loss = model(x)\nscaler.scale(loss).backward()\nscaler.step(optimizer)\nscaler.update()\n```\n**التسريع: 1.8-2.5x على وحدات GPU الحديثة**\n\n### 2. Gradient Checkpointing\n```python\nmodel.gradient_checkpointing_enable()\n```\n**الذاكرة: -40%، السرعة: -15%** — يستحق العناء للنماذج الكبيرة\n\n### 3. DataLoader الأمثل\n```python\nDataLoader(dataset, num_workers=4, pin_memory=True, persistent_workers=True)\n```\n\n### 4. torch.compile() (PyTorch 2.0+)\n```python\nmodel = torch.compile(model)\n```\n**التسريع: 1.2-2x حسب النموذج**\n\n### 5-10. OneCycleLR، وقص التدرجات، والمحسّنات المدمجة، وتدفقات CUDA، وtorch.backends.cudnn.benchmark، والانتباه الموفّر للذاكرة."},
  "bert-fine-tuning-production-nlp": {"en":"## The Fine-Tuning Recipe\n\n```python\nfrom transformers import BertForSequenceClassification, AdamW\n\nmodel = BertForSequenceClassification.from_pretrained(\n    'bert-base-multilingual-cased',  # for AR/FR/EN\n    num_labels=3\n)\n\n# Discriminative learning rates\noptimizer = AdamW([\n    {'params': model.bert.embeddings.parameters(), 'lr': 1e-5},\n    {'params': model.bert.encoder.layer[:6].parameters(), 'lr': 2e-5},\n    {'params': model.bert.encoder.layer[6:].parameters(), 'lr': 3e-5},\n    {'params': model.classifier.parameters(), 'lr': 5e-5},\n])\n```\n\n## Key Lessons\n\n1. Start with LR=2e-5, batch=16, epochs=3-5\n2. Warmup for 10% of steps prevents catastrophic forgetting\n3. For Arabic: use CAMeL-BERT or AraBERT, not mBERT\n4. ONNX export: 3x faster inference, no PyTorch needed","fr":"## La recette de fine-tuning\n\n```python\nfrom transformers import BertForSequenceClassification, AdamW\n\nmodel = BertForSequenceClassification.from_pretrained(\n    'bert-base-multilingual-cased',  # for AR/FR/EN\n    num_labels=3\n)\n\n# Discriminative learning rates\noptimizer = AdamW([\n    {'params': model.bert.embeddings.parameters(), 'lr': 1e-5},\n    {'params': model.bert.encoder.layer[:6].parameters(), 'lr': 2e-5},\n    {'params': model.bert.encoder.layer[6:].parameters(), 'lr': 3e-5},\n    {'params': model.classifier.parameters(), 'lr': 5e-5},\n])\n```\n\n## Leçons clés\n\n1. Commencez avec LR=2e-5, batch=16, epochs=3-5\n2. Un warmup sur 10 % des étapes évite l'oubli catastrophique\n3. Pour l'arabe : utilisez CAMeL-BERT ou AraBERT, pas mBERT\n4. Export ONNX : inférence 3x plus rapide, sans PyTorch","ar":"## وصفة الضبط الدقيق\n\n```python\nfrom transformers import BertForSequenceClassification, AdamW\n\nmodel = BertForSequenceClassification.from_pretrained(\n    'bert-base-multilingual-cased',  # for AR/FR/EN\n    num_labels=3\n)\n\n# Discriminative learning rates\noptimizer = AdamW([\n    {'params': model.bert.embeddings.parameters(), 'lr': 1e-5},\n    {'params': model.bert.encoder.layer[:6].parameters(), 'lr': 2e-5},\n    {'params': model.bert.encoder.layer[6:].parameters(), 'lr': 3e-5},\n    {'params': model.classifier.parameters(), 'lr': 5e-5},\n])\n```\n\n## دروس رئيسية\n\n1. ابدأ بـ LR=2e-5 وbatch=16 وepochs=3-5\n2. الإحماء على 10% من الخطوات يمنع النسيان الكارثي\n3. للعربية: استخدم CAMeL-BERT أو AraBERT وليس mBERT\n4. تصدير ONNX: استدلال أسرع بثلاث مرات دون الحاجة إلى PyTorch"},
  "rag-system-production-langchain": {"en":"## Architecture\n\n```\nDocuments → Chunker → Embedder → Pinecone\n                                    ↓\nUser Query → Embedder → Pinecone Search → Reranker → LLM → Answer\n```\n\n## Chunking Strategy\n\n```python\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\n\nsplitter = RecursiveCharacterTextSplitter(\n    chunk_size=512,\n    chunk_overlap=64,\n    separators=['\\n\\n', '\\n', '. ', ' '],\n)\n```\n\n## Hybrid Search (BM25 + Dense)\n\nBM25 for keyword match + cosine similarity for semantic — combine with RRF:\n```python\nrrf_score = sum(1/(k + rank_i) for rank_i in [bm25_rank, dense_rank])\n```\n\n## Hallucination Mitigation\n\n- Cite source chunks in the answer\n- Set temperature=0 for factual queries\n- Check if answer tokens overlap with context (RAGAS faithfulness metric)","fr":"## Architecture\n\n```\nDocuments → Chunker → Embedder → Pinecone\n                                    ↓\nUser Query → Embedder → Pinecone Search → Reranker → LLM → Answer\n```\n\n## Stratégie de chunking\n\n```python\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\n\nsplitter = RecursiveCharacterTextSplitter(\n    chunk_size=512,\n    chunk_overlap=64,\n    separators=['\\n\\n', '\\n', '. ', ' '],\n)\n```\n\n## Recherche hybride (BM25 + dense)\n\nBM25 pour la correspondance de mots-clés + similarité cosinus pour le sémantique — combinées via RRF :\n```python\nrrf_score = sum(1/(k + rank_i) for rank_i in [bm25_rank, dense_rank])\n```\n\n## Atténuation des hallucinations\n\n- Citez les chunks sources dans la réponse\n- Mettez temperature=0 pour les requêtes factuelles\n- Vérifiez le recouvrement des tokens de réponse avec le contexte (métrique de fidélité RAGAS)","ar":"## المعمارية\n\n```\nDocuments → Chunker → Embedder → Pinecone\n                                    ↓\nUser Query → Embedder → Pinecone Search → Reranker → LLM → Answer\n```\n\n## استراتيجية التقطيع\n\n```python\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\n\nsplitter = RecursiveCharacterTextSplitter(\n    chunk_size=512,\n    chunk_overlap=64,\n    separators=['\\n\\n', '\\n', '. ', ' '],\n)\n```\n\n## البحث الهجين (BM25 + كثيف)\n\nBM25 لمطابقة الكلمات المفتاحية + تشابه جيب التمام للدلالة — يُدمجان عبر RRF:\n```python\nrrf_score = sum(1/(k + rank_i) for rank_i in [bm25_rank, dense_rank])\n```\n\n## التخفيف من الهلوسة\n\n- استشهد بالمقاطع المصدرية في الإجابة\n- اضبط temperature=0 للاستعلامات الواقعية\n- تحقق من تداخل رموز الإجابة مع السياق (مقياس الأمانة RAGAS)"},
  "arabic-nlp-arabert-guide": {"en":"## The Arabic NLP Landscape\n\n### Best Models (2025)\n\n| Model | Best For |\n|-------|----------|\n| AraBERT v0.2 | Classification, NER |\n| CAMeL-BERT | Dialectal Arabic |\n| AraGPT2 | Text generation |\n| Jais-13b | Instruction following |\n\n## Preprocessing Pipeline\n\n```python\nimport re\n\ndef preprocess_arabic(text):\n    # Remove diacritics (tashkeel)\n    text = re.sub(r'[\\u064B-\\u065F]', '', text)\n    # Normalize alef variants\n    text = re.sub(r'[أإآا]', 'ا', text)\n    # Remove tatweel\n    text = re.sub(r'\\u0640', '', text)\n    return text.strip()\n```\n\n## Dialectal Challenges\n\nMSA (Modern Standard Arabic) models perform poorly on:\n- Moroccan Darija\n- Egyptian Arabic\n- Gulf Arabic\n\nSolution: Fine-tune on dialect-specific data or use CAMeL-BERT.","fr":"## Le paysage du NLP arabe\n\n### Meilleurs modèles (2025)\n\n| Modèle | Idéal pour |\n|-------|----------|\n| AraBERT v0.2 | Classification, NER |\n| CAMeL-BERT | Arabe dialectal |\n| AraGPT2 | Génération de texte |\n| Jais-13b | Suivi d'instructions |\n\n## Pipeline de prétraitement\n\n```python\nimport re\n\ndef preprocess_arabic(text):\n    # Remove diacritics (tashkeel)\n    text = re.sub(r'[\\u064B-\\u065F]', '', text)\n    # Normalize alef variants\n    text = re.sub(r'[أإآا]', 'ا', text)\n    # Remove tatweel\n    text = re.sub(r'\\u0640', '', text)\n    return text.strip()\n```\n\n## Défis dialectaux\n\nLes modèles MSA (arabe standard moderne) sont peu performants sur :\n- le darija marocain\n- l'arabe égyptien\n- l'arabe du Golfe\n\nSolution : fine-tuner sur des données dialectales spécifiques ou utiliser CAMeL-BERT.","ar":"## مشهد معالجة اللغة العربية\n\n### أفضل النماذج (2025)\n\n| النموذج | الأفضل لـ |\n|-------|----------|\n| AraBERT v0.2 | التصنيف، NER |\n| CAMeL-BERT | العربية العامية |\n| AraGPT2 | توليد النص |\n| Jais-13b | اتّباع التعليمات |\n\n## خط المعالجة المسبقة\n\n```python\nimport re\n\ndef preprocess_arabic(text):\n    # Remove diacritics (tashkeel)\n    text = re.sub(r'[\\u064B-\\u065F]', '', text)\n    # Normalize alef variants\n    text = re.sub(r'[أإآا]', 'ا', text)\n    # Remove tatweel\n    text = re.sub(r'\\u0640', '', text)\n    return text.strip()\n```\n\n## تحديات اللهجات\n\nنماذج العربية الفصحى الحديثة (MSA) ضعيفة الأداء على:\n- الدارجة المغربية\n- العربية المصرية\n- اللهجة الخليجية\n\nالحل: الضبط الدقيق على بيانات لهجة محددة أو استخدام CAMeL-BERT."},
  "llm-prompt-engineering-guide": {"en":"## Pattern 1: Chain-of-Thought (CoT)\n\nAdd \"Let's think step by step\" to complex reasoning tasks. Improves accuracy by 20-40% on math and logic.\n\n## Pattern 2: Few-Shot with Negative Examples\n\n```\nCorrect: \"The product arrived damaged\" → NEGATIVE\nCorrect: \"Excellent quality, fast shipping\" → POSITIVE\nIncorrect: \"It's okay I guess\" → [CLASSIFY]\n```\n\n## Pattern 3: JSON Mode\n\n```python\nresponse = client.chat.completions.create(\n    model='gpt-4o',\n    response_format={'type': 'json_object'},\n    messages=[{'role': 'user', 'content': 'Extract entities from: ...' }]\n)\n```\n\n## Pattern 4: Persona + Constraint\n\n\"You are a senior ML engineer. Explain gradient descent in under 3 sentences. Use no math notation.\"\n\n## Pattern 5: Self-Consistency\n\nRun the same prompt 5 times, take majority vote. Reduces error rate by ~30% on reasoning tasks.","fr":"## Pattern 1 : Chain-of-Thought (CoT)\n\nAjoutez « Réfléchissons étape par étape » aux tâches de raisonnement complexes. Améliore la précision de 20-40 % en maths et logique.\n\n## Pattern 2 : Few-shot avec exemples négatifs\n\n```\nCorrect: \"The product arrived damaged\" → NEGATIVE\nCorrect: \"Excellent quality, fast shipping\" → POSITIVE\nIncorrect: \"It's okay I guess\" → [CLASSIFY]\n```\n\n## Pattern 3 : Mode JSON\n\n```python\nresponse = client.chat.completions.create(\n    model='gpt-4o',\n    response_format={'type': 'json_object'},\n    messages=[{'role': 'user', 'content': 'Extract entities from: ...' }]\n)\n```\n\n## Pattern 4 : Persona + contrainte\n\n« Vous êtes un ingénieur ML senior. Expliquez la descente de gradient en moins de 3 phrases. N'utilisez aucune notation mathématique. »\n\n## Pattern 5 : Auto-cohérence\n\nLancez le même prompt 5 fois et prenez le vote majoritaire. Réduit le taux d'erreur d'environ 30 % sur les tâches de raisonnement.","ar":"## النمط 1: سلسلة التفكير (CoT)\n\nأضف «لنفكّر خطوة بخطوة» لمهام الاستدلال المعقدة. يحسّن الدقة بنسبة 20-40% في الرياضيات والمنطق.\n\n## النمط 2: Few-Shot مع أمثلة سلبية\n\n```\nCorrect: \"The product arrived damaged\" → NEGATIVE\nCorrect: \"Excellent quality, fast shipping\" → POSITIVE\nIncorrect: \"It's okay I guess\" → [CLASSIFY]\n```\n\n## النمط 3: وضع JSON\n\n```python\nresponse = client.chat.completions.create(\n    model='gpt-4o',\n    response_format={'type': 'json_object'},\n    messages=[{'role': 'user', 'content': 'Extract entities from: ...' }]\n)\n```\n\n## النمط 4: شخصية + قيد\n\n«أنت مهندس تعلم آلي خبير. اشرح الانحدار التدرجي في أقل من 3 جمل. لا تستخدم أي رموز رياضية.»\n\n## النمط 5: الاتساق الذاتي\n\nشغّل المطالبة نفسها 5 مرات وخذ تصويت الأغلبية. يقلل معدل الخطأ بنحو 30% في مهام الاستدلال."},
  "whatsapp-ai-agent-n8n-ollama": {"en":"## Architecture Overview\n\n```\nWhatsApp Business API\n        ↓\n    n8n Webhook\n        ↓\n   Ollama (Llama 3.1 8B) ← Product DB (Postgres)\n        ↓\n   Conversation Memory (Redis)\n        ↓\n  WhatsApp Reply\n```\n\n## n8n Workflow Nodes\n\n1. **Webhook** — receives WhatsApp messages\n2. **Redis GET** — load conversation history (last 10 turns)\n3. **Postgres** — retrieve product details by name/category\n4. **Ollama Chat** — generate response with context\n5. **Redis SET** — save updated conversation\n6. **HTTP Request** — send reply via WhatsApp Cloud API\n\n## System Prompt\n\n```\nYou are Amina, a friendly bilingual (FR/AR) customer service agent for [Store]. \nAlways respond in the same language the customer uses.\nOnly recommend products from the provided catalog.\nNever invent prices or availability.\n```\n\n## Conversation Memory Design\n\nStore as JSON array in Redis with 24h TTL:\n```json\n[{\"role\":\"user\",\"content\":\"...\"},\n {\"role\":\"assistant\",\"content\":\"...\"}]\n```","fr":"## Vue d'ensemble de l'architecture\n\n```\nWhatsApp Business API\n        ↓\n    n8n Webhook\n        ↓\n   Ollama (Llama 3.1 8B) ← Product DB (Postgres)\n        ↓\n   Conversation Memory (Redis)\n        ↓\n  WhatsApp Reply\n```\n\n## Nœuds du workflow n8n\n\n1. **Webhook** — reçoit les messages WhatsApp\n2. **Redis GET** — charge l'historique de conversation (10 derniers tours)\n3. **Postgres** — récupère les détails produit par nom/catégorie\n4. **Ollama Chat** — génère la réponse avec le contexte\n5. **Redis SET** — enregistre la conversation mise à jour\n6. **HTTP Request** — envoie la réponse via WhatsApp Cloud API\n\n## Prompt système\n\n```\nYou are Amina, a friendly bilingual (FR/AR) customer service agent for [Store]. \nAlways respond in the same language the customer uses.\nOnly recommend products from the provided catalog.\nNever invent prices or availability.\n```\n\n## Conception de la mémoire de conversation\n\nStockée comme tableau JSON dans Redis avec un TTL de 24 h :\n```json\n[{\"role\":\"user\",\"content\":\"...\"},\n {\"role\":\"assistant\",\"content\":\"...\"}]\n```","ar":"## نظرة عامة على المعمارية\n\n```\nWhatsApp Business API\n        ↓\n    n8n Webhook\n        ↓\n   Ollama (Llama 3.1 8B) ← Product DB (Postgres)\n        ↓\n   Conversation Memory (Redis)\n        ↓\n  WhatsApp Reply\n```\n\n## عُقد سير عمل n8n\n\n1. **Webhook** — يستقبل رسائل WhatsApp\n2. **Redis GET** — تحميل سجل المحادثة (آخر 10 أدوار)\n3. **Postgres** — جلب تفاصيل المنتج حسب الاسم/الفئة\n4. **Ollama Chat** — توليد الرد مع السياق\n5. **Redis SET** — حفظ المحادثة المحدّثة\n6. **HTTP Request** — إرسال الرد عبر WhatsApp Cloud API\n\n## موجّه النظام\n\n```\nYou are Amina, a friendly bilingual (FR/AR) customer service agent for [Store]. \nAlways respond in the same language the customer uses.\nOnly recommend products from the provided catalog.\nNever invent prices or availability.\n```\n\n## تصميم ذاكرة المحادثة\n\nتُخزَّن كمصفوفة JSON في Redis بمدة صلاحية 24 ساعة:\n```json\n[{\"role\":\"user\",\"content\":\"...\"},\n {\"role\":\"assistant\",\"content\":\"...\"}]\n```"},
  "n8n-ai-automation-workflows-guide": {"en":"## Workflow 1: Lead Qualification Agent\n\n**Trigger**: New lead form submission\n**Process**: GPT-4o scores the lead (1-10) based on company size, budget, urgency\n**Output**: Automatically assigns to sales rep, sends personalized intro email\n**ROI**: 60% reduction in manual lead screening time\n\n## Workflow 2: Document Intelligence\n\n**Trigger**: New PDF uploaded to Google Drive\n**Process**: OCR → Mistral AI extracts structured data → validates against schema\n**Output**: JSON sent to CRM/ERP\n**ROI**: Replaced 2 hours/day of manual data entry\n\n## Workflow 3: Social Media Monitor\n\n**Trigger**: Brand mention on Twitter/LinkedIn\n**Process**: Sentiment analysis → categorize (complaint/praise/question)\n**Output**: Urgent complaints → Slack alert, others → weekly digest\n\n## Workflow 4: Inventory Alert System\n\n**Trigger**: Daily cron at 8am\n**Process**: Query stock levels → LLM generates reorder recommendations\n**Output**: WhatsApp message to procurement manager\n\n## Workflow 5: Customer Support Triage\n\nAutomatic ticket categorization and priority assignment using Claude.","fr":"## Workflow 1 : agent de qualification de leads\n\n**Déclencheur** : nouvelle soumission de formulaire de lead\n**Processus** : GPT-4o note le lead (1-10) selon la taille de l'entreprise, le budget, l'urgence\n**Sortie** : assigne automatiquement à un commercial, envoie un e-mail d'introduction personnalisé\n**ROI** : -60 % de temps de tri manuel des leads\n\n## Workflow 2 : intelligence documentaire\n\n**Déclencheur** : nouveau PDF déposé sur Google Drive\n**Processus** : OCR → Mistral AI extrait des données structurées → validation par schéma\n**Sortie** : JSON envoyé au CRM/ERP\n**ROI** : a remplacé 2 heures/jour de saisie manuelle\n\n## Workflow 3 : veille des réseaux sociaux\n\n**Déclencheur** : mention de la marque sur Twitter/LinkedIn\n**Processus** : analyse de sentiment → catégorisation (plainte/éloge/question)\n**Sortie** : plaintes urgentes → alerte Slack, le reste → résumé hebdomadaire\n\n## Workflow 4 : système d'alerte de stock\n\n**Déclencheur** : cron quotidien à 8h\n**Processus** : interroge les niveaux de stock → le LLM génère des recommandations de réapprovisionnement\n**Sortie** : message WhatsApp au responsable des achats\n\n## Workflow 5 : triage du support client\n\nCatégorisation automatique des tickets et attribution de priorité avec Claude.","ar":"## التدفق 1: وكيل تأهيل العملاء المحتملين\n\n**المُطلِق**: إرسال نموذج عميل محتمل جديد\n**العملية**: يقيّم GPT-4o العميل (1-10) بناءً على حجم الشركة والميزانية والإلحاح\n**المخرجات**: يُسنِد تلقائياً إلى مندوب مبيعات ويرسل بريد تعريف مخصص\n**العائد**: خفض 60% من وقت فرز العملاء يدوياً\n\n## التدفق 2: ذكاء الوثائق\n\n**المُطلِق**: رفع ملف PDF جديد إلى Google Drive\n**العملية**: OCR → يستخرج Mistral AI بيانات منظمة → التحقق وفق المخطط\n**المخرجات**: JSON يُرسَل إلى CRM/ERP\n**العائد**: استبدل ساعتين يومياً من الإدخال اليدوي\n\n## التدفق 3: مراقبة وسائل التواصل\n\n**المُطلِق**: ذكر العلامة على Twitter/LinkedIn\n**العملية**: تحليل المشاعر → التصنيف (شكوى/إشادة/سؤال)\n**المخرجات**: الشكاوى العاجلة → تنبيه Slack، والبقية → ملخص أسبوعي\n\n## التدفق 4: نظام تنبيه المخزون\n\n**المُطلِق**: cron يومي الساعة 8 صباحاً\n**العملية**: الاستعلام عن مستويات المخزون → يولّد LLM توصيات إعادة الطلب\n**المخرجات**: رسالة WhatsApp إلى مدير المشتريات\n\n## التدفق 5: فرز دعم العملاء\n\nتصنيف تلقائي للتذاكر وتعيين الأولوية باستخدام Claude."},
  "multi-agent-system-design": {"en":"## Architecture Patterns\n\n### 1. Orchestrator-Worker\nBest for: Clear task decomposition, parallel subtasks\n```\nOrchestrator (GPT-4o)\n├── Researcher Agent (Perplexity/Tavily)\n├── Writer Agent (Claude)\n└── Editor Agent (GPT-4o-mini)\n```\n\n### 2. Peer-to-Peer (Debate)\nBest for: High-stakes decisions requiring multiple perspectives\nAgents critique each other's outputs until consensus.\n\n### 3. Hierarchical\nBest for: Enterprise workflows with approvals\nCEO Agent → Manager Agents → Worker Agents\n\n## Communication Protocol\n\nUse structured JSON handoffs between agents:\n```json\n{\n  \"task_id\": \"uuid\",\n  \"agent_from\": \"researcher\",\n  \"agent_to\": \"writer\",\n  \"context\": {...},\n  \"output\": {...},\n  \"confidence\": 0.92\n}\n```\n\n## Failure Recovery\n\nAlways implement: retry with backoff, fallback agents, timeout circuits.","fr":"## Patterns d'architecture\n\n### 1. Orchestrateur-worker\nIdéal pour : décomposition claire des tâches, sous-tâches parallèles\n```\nOrchestrator (GPT-4o)\n├── Researcher Agent (Perplexity/Tavily)\n├── Writer Agent (Claude)\n└── Editor Agent (GPT-4o-mini)\n```\n\n### 2. Peer-to-peer (débat)\nIdéal pour : décisions à fort enjeu nécessitant plusieurs points de vue\nLes agents critiquent mutuellement leurs sorties jusqu'au consensus.\n\n### 3. Hiérarchique\nIdéal pour : workflows d'entreprise avec validations\nCEO Agent → Manager Agents → Worker Agents\n\n## Protocole de communication\n\nUtilisez des handoffs JSON structurés entre agents :\n```json\n{\n  \"task_id\": \"uuid\",\n  \"agent_from\": \"researcher\",\n  \"agent_to\": \"writer\",\n  \"context\": {...},\n  \"output\": {...},\n  \"confidence\": 0.92\n}\n```\n\n## Récupération sur échec\n\nImplémentez toujours : retry avec backoff, agents de secours, circuits de timeout.","ar":"## أنماط المعمارية\n\n### 1. المنسّق-العامل\nالأفضل لـ: التحليل الواضح للمهام، والمهام الفرعية المتوازية\n```\nOrchestrator (GPT-4o)\n├── Researcher Agent (Perplexity/Tavily)\n├── Writer Agent (Claude)\n└── Editor Agent (GPT-4o-mini)\n```\n\n### 2. Peer-to-Peer (نقاش)\nالأفضل لـ: القرارات الحرجة التي تتطلب وجهات نظر متعددة\nيَنقد الوكلاء مخرجات بعضهم حتى الوصول إلى توافق.\n\n### 3. الهرمي\nالأفضل لـ: تدفقات عمل المؤسسات التي تتطلب موافقات\nCEO Agent → Manager Agents → Worker Agents\n\n## بروتوكول الاتصال\n\nاستخدم عمليات تسليم JSON منظمة بين الوكلاء:\n```json\n{\n  \"task_id\": \"uuid\",\n  \"agent_from\": \"researcher\",\n  \"agent_to\": \"writer\",\n  \"context\": {...},\n  \"output\": {...},\n  \"confidence\": 0.92\n}\n```\n\n## التعافي من الأعطال\n\nنفّذ دائماً: إعادة المحاولة مع backoff، ووكلاء احتياطيين، ودوائر مهلة زمنية."},
  "mlops-pipeline-from-scratch": {"en":"## The Full MLOps Stack\n\n```\nGit + DVC (code + data versioning)\n      ↓\nGitHub Actions (CI: tests, linting)\n      ↓\nMLflow (experiment tracking)\n      ↓\nMLflow Model Registry (staging → production)\n      ↓\nFastAPI + Docker (serving)\n      ↓\nPrometheus + Grafana (monitoring)\n```\n\n## DVC Data Versioning\n\n```bash\ndvc init\ndvc add data/train.csv\ndvc push  # to S3/GCS\ngit add data/train.csv.dvc\ngit commit -m \"add training data v2\"\n```\n\n## MLflow Experiment Tracking\n\n```python\nimport mlflow\n\nwith mlflow.start_run():\n    mlflow.log_params(params)\n    mlflow.log_metrics({'auc': auc, 'f1': f1})\n    mlflow.sklearn.log_model(model, 'model')\n```\n\n## Automated Retraining Gate\n\nIf production AUC drops >2% → trigger retraining pipeline automatically.","fr":"## La stack MLOps complète\n\n```\nGit + DVC (code + data versioning)\n      ↓\nGitHub Actions (CI: tests, linting)\n      ↓\nMLflow (experiment tracking)\n      ↓\nMLflow Model Registry (staging → production)\n      ↓\nFastAPI + Docker (serving)\n      ↓\nPrometheus + Grafana (monitoring)\n```\n\n## Versioning des données avec DVC\n\n```bash\ndvc init\ndvc add data/train.csv\ndvc push  # to S3/GCS\ngit add data/train.csv.dvc\ngit commit -m \"add training data v2\"\n```\n\n## Suivi des expériences avec MLflow\n\n```python\nimport mlflow\n\nwith mlflow.start_run():\n    mlflow.log_params(params)\n    mlflow.log_metrics({'auc': auc, 'f1': f1})\n    mlflow.sklearn.log_model(model, 'model')\n```\n\n## Porte de réentraînement automatique\n\nSi l'AUC en production chute de >2 % → déclenche automatiquement le pipeline de réentraînement.","ar":"## مكدّس MLOps الكامل\n\n```\nGit + DVC (code + data versioning)\n      ↓\nGitHub Actions (CI: tests, linting)\n      ↓\nMLflow (experiment tracking)\n      ↓\nMLflow Model Registry (staging → production)\n      ↓\nFastAPI + Docker (serving)\n      ↓\nPrometheus + Grafana (monitoring)\n```\n\n## إصدار البيانات باستخدام DVC\n\n```bash\ndvc init\ndvc add data/train.csv\ndvc push  # to S3/GCS\ngit add data/train.csv.dvc\ngit commit -m \"add training data v2\"\n```\n\n## تتبع التجارب باستخدام MLflow\n\n```python\nimport mlflow\n\nwith mlflow.start_run():\n    mlflow.log_params(params)\n    mlflow.log_metrics({'auc': auc, 'f1': f1})\n    mlflow.sklearn.log_model(model, 'model')\n```\n\n## بوابة إعادة التدريب التلقائية\n\nإذا انخفض AUC في الإنتاج بأكثر من 2% → يُطلَق خط أنابيب إعادة التدريب تلقائياً."},
  "model-drift-monitoring-production": {"en":"## Two Types of Drift\n\n1. **Data Drift**: Input feature distribution changes (P(X) shifts)\n2. **Concept Drift**: The relationship between features and target changes (P(Y|X) shifts)\n\nData drift is easier to detect. Concept drift requires labeled data.\n\n## Detection with Evidently AI\n\n```python\nfrom evidently.test_suite import TestSuite\nfrom evidently.tests import TestColumnDrift\n\ntest_suite = TestSuite(tests=[\n    TestColumnDrift(column_name='TransactionAmt'),\n    TestColumnDrift(column_name='card1'),\n])\ntest_suite.run(reference_data=ref_df, current_data=cur_df)\n```\n\n## Statistical Tests\n\n- **KS Test**: Continuous features\n- **Chi-squared**: Categorical features\n- **PSI** (Population Stability Index): Both — PSI > 0.2 = critical drift\n\n## Alerting Strategy\n\n- PSI > 0.1: Warning (monitor closely)\n- PSI > 0.2: Alert (schedule retraining)\n- AUC drop > 3%: Emergency retrain","fr":"## Deux types de dérive\n\n1. **Dérive des données** : la distribution des features d'entrée change (P(X) se décale)\n2. **Dérive du concept** : la relation entre features et cible change (P(Y|X) se décale)\n\nLa dérive des données est plus facile à détecter. La dérive du concept nécessite des données étiquetées.\n\n## Détection avec Evidently AI\n\n```python\nfrom evidently.test_suite import TestSuite\nfrom evidently.tests import TestColumnDrift\n\ntest_suite = TestSuite(tests=[\n    TestColumnDrift(column_name='TransactionAmt'),\n    TestColumnDrift(column_name='card1'),\n])\ntest_suite.run(reference_data=ref_df, current_data=cur_df)\n```\n\n## Tests statistiques\n\n- **Test KS** : features continues\n- **Khi-deux** : features catégorielles\n- **PSI** (Population Stability Index) : les deux — PSI > 0.2 = dérive critique\n\n## Stratégie d'alerte\n\n- PSI > 0.1 : avertissement (surveiller de près)\n- PSI > 0.2 : alerte (planifier un réentraînement)\n- Chute d'AUC > 3 % : réentraînement d'urgence","ar":"## نوعان من الانجراف\n\n1. **انجراف البيانات**: تتغيّر توزيعة ميزات الإدخال (يتحرّك P(X))\n2. **انجراف المفهوم**: تتغيّر العلاقة بين الميزات والهدف (يتحرّك P(Y|X))\n\nانجراف البيانات أسهل في الكشف. أما انجراف المفهوم فيتطلب بيانات موسومة.\n\n## الكشف باستخدام Evidently AI\n\n```python\nfrom evidently.test_suite import TestSuite\nfrom evidently.tests import TestColumnDrift\n\ntest_suite = TestSuite(tests=[\n    TestColumnDrift(column_name='TransactionAmt'),\n    TestColumnDrift(column_name='card1'),\n])\ntest_suite.run(reference_data=ref_df, current_data=cur_df)\n```\n\n## الاختبارات الإحصائية\n\n- **اختبار KS**: للميزات المستمرة\n- **مربع كاي**: للميزات الفئوية\n- **PSI** (مؤشر استقرار المجتمع): لكليهما — PSI > 0.2 = انجراف حرج\n\n## استراتيجية التنبيه\n\n- PSI > 0.1: تحذير (راقب عن كثب)\n- PSI > 0.2: تنبيه (جدوِل إعادة التدريب)\n- انخفاض AUC > 3%: إعادة تدريب طارئة"},
  "fastapi-ml-deployment-guide": {"en":"## Minimal Production FastAPI\n\n```python\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\nimport joblib\nimport numpy as np\n\napp = FastAPI()\nmodel = joblib.load('model.pkl')\n\nclass PredictRequest(BaseModel):\n    features: list[float]\n    \nclass PredictResponse(BaseModel):\n    prediction: float\n    probability: float\n    model_version: str = 'v1.2'\n\n@app.post('/predict', response_model=PredictResponse)\nasync def predict(req: PredictRequest):\n    X = np.array(req.features).reshape(1, -1)\n    pred = model.predict(X)[0]\n    prob = model.predict_proba(X)[0].max()\n    return PredictResponse(prediction=float(pred), probability=float(prob))\n\n@app.get('/health')\ndef health(): return {'status': 'ok', 'model': 'loaded'}\n```\n\n## Production Checklist\n\n- [ ] Input validation with Pydantic\n- [ ] Health check endpoint\n- [ ] Request logging (structlog)\n- [ ] Rate limiting (slowapi)\n- [ ] Model warmup on startup\n- [ ] Graceful shutdown\n- [ ] Docker with multi-stage build","fr":"## FastAPI de production minimal\n\n```python\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\nimport joblib\nimport numpy as np\n\napp = FastAPI()\nmodel = joblib.load('model.pkl')\n\nclass PredictRequest(BaseModel):\n    features: list[float]\n    \nclass PredictResponse(BaseModel):\n    prediction: float\n    probability: float\n    model_version: str = 'v1.2'\n\n@app.post('/predict', response_model=PredictResponse)\nasync def predict(req: PredictRequest):\n    X = np.array(req.features).reshape(1, -1)\n    pred = model.predict(X)[0]\n    prob = model.predict_proba(X)[0].max()\n    return PredictResponse(prediction=float(pred), probability=float(prob))\n\n@app.get('/health')\ndef health(): return {'status': 'ok', 'model': 'loaded'}\n```\n\n## Checklist de production\n\n- [ ] Validation des entrées avec Pydantic\n- [ ] Endpoint de health check\n- [ ] Journalisation des requêtes (structlog)\n- [ ] Rate limiting (slowapi)\n- [ ] Warmup du modèle au démarrage\n- [ ] Arrêt gracieux\n- [ ] Docker avec build multi-stage","ar":"## أبسط FastAPI إنتاجي\n\n```python\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\nimport joblib\nimport numpy as np\n\napp = FastAPI()\nmodel = joblib.load('model.pkl')\n\nclass PredictRequest(BaseModel):\n    features: list[float]\n    \nclass PredictResponse(BaseModel):\n    prediction: float\n    probability: float\n    model_version: str = 'v1.2'\n\n@app.post('/predict', response_model=PredictResponse)\nasync def predict(req: PredictRequest):\n    X = np.array(req.features).reshape(1, -1)\n    pred = model.predict(X)[0]\n    prob = model.predict_proba(X)[0].max()\n    return PredictResponse(prediction=float(pred), probability=float(prob))\n\n@app.get('/health')\ndef health(): return {'status': 'ok', 'model': 'loaded'}\n```\n\n## قائمة مراجعة الإنتاج\n\n- [ ] التحقق من المدخلات باستخدام Pydantic\n- [ ] نقطة نهاية فحص صحي\n- [ ] تسجيل الطلبات (structlog)\n- [ ] تحديد المعدل (slowapi)\n- [ ] تسخين النموذج عند الإقلاع\n- [ ] إيقاف سلس\n- [ ] Docker مع بناء متعدد المراحل"},
  "kaggle-competition-strategy-guide": {"en":"## Week 1: Foundation\n\n1. **EDA** (2 days): Distribution analysis, missing values, target leakage check\n2. **Baseline** (1 day): Simple model, establish CV score\n3. **Reproducibility** (1 day): Fix seeds, set up CV pipeline\n\n## Week 2-3: Feature Engineering\n\nWork in sprints of 2-3 features, commit only if CV improves:\n- Start with domain-obvious features\n- Add aggregations by key entity columns\n- Extract date/time parts\n- Compute pairwise interactions\n\n## Week 4: Ensemble\n\n1. Train 5-10 diverse base models\n2. Hold out 20% as ensemble validation set\n3. Use simple Optuna-tuned weighted average\n4. Stacking only if you have time for proper CV\n\n## Last 3 Days: Final Push\n\n- Submit 3-4 ensemble variants\n- Select final based on CV+LB balance\n- Don't overfit to leaderboard (keep 2 submissions in reserve)","fr":"## Semaine 1 : fondations\n\n1. **EDA** (2 jours) : analyse des distributions, valeurs manquantes, vérification de la fuite de cible\n2. **Baseline** (1 jour) : modèle simple, établir le score CV\n3. **Reproductibilité** (1 jour) : fixer les graines, mettre en place le pipeline CV\n\n## Semaines 2-3 : feature engineering\n\nTravaillez par sprints de 2-3 caractéristiques, ne committez que si la CV s'améliore :\n- Commencez par les caractéristiques évidentes du domaine\n- Ajoutez des agrégations par colonnes d'entité clés\n- Extrayez les composants date/heure\n- Calculez les interactions par paires\n\n## Semaine 4 : ensemble\n\n1. Entraînez 5-10 modèles de base variés\n2. Réservez 20 % comme jeu de validation d'ensemble\n3. Utilisez une moyenne pondérée simple réglée avec Optuna\n4. Le stacking seulement si vous avez le temps pour une CV correcte\n\n## 3 derniers jours : sprint final\n\n- Soumettez 3-4 variantes d'ensemble\n- Choisissez la finale selon l'équilibre CV+LB\n- Ne surajustez pas au leaderboard (gardez 2 soumissions en réserve)","ar":"## الأسبوع 1: الأساس\n\n1. **EDA** (يومان): تحليل التوزيعات، القيم المفقودة، فحص تسرب الهدف\n2. **خط الأساس** (يوم): نموذج بسيط، تثبيت درجة CV\n3. **قابلية إعادة الإنتاج** (يوم): تثبيت البذور وإعداد خط أنابيب CV\n\n## الأسبوعان 2-3: هندسة الميزات\n\nاعمل بسباقات من 2-3 ميزات، ولا تعتمد إلا إذا تحسّنت CV:\n- ابدأ بالميزات الواضحة في المجال\n- أضف تجميعات حسب أعمدة الكيانات الرئيسية\n- استخرج مكوّنات التاريخ/الوقت\n- احسب التفاعلات الثنائية\n\n## الأسبوع 4: المجموعة (Ensemble)\n\n1. درّب 5-10 نماذج أساس متنوعة\n2. احجز 20% كمجموعة تحقق للـ ensemble\n3. استخدم متوسطاً مرجّحاً بسيطاً مضبوطاً بـ Optuna\n4. لا تلجأ للـ stacking إلا إذا كان لديك وقت لإجراء CV سليم\n\n## آخر 3 أيام: الدفعة الأخيرة\n\n- قدّم 3-4 متغيرات من المجموعة\n- اختر النهائي بناءً على توازن CV+LB\n- لا تفرط في ملاءمة لوحة الصدارة (احتفظ بتقديمين احتياطيين)"},
  "time-series-forecasting-production": {"en":"## Method Selection\n\n| Scenario | Best Method |\n|----------|-------------|\n| Few series, clean data | Prophet, SARIMA |\n| Many series (1000+) | LightGBM with lag features |\n| Complex seasonality | N-BEATS, TFT |\n| Real-time, low latency | Simple exponential smoothing |\n\n## LightGBM for Time Series\n\n```python\ndef make_features(df, lags=[1,7,14,28], windows=[7,14,28]):\n    for lag in lags:\n        df[f'lag_{lag}'] = df['target'].shift(lag)\n    for w in windows:\n        df[f'rolling_mean_{w}'] = df['target'].shift(1).rolling(w).mean()\n        df[f'rolling_std_{w}'] = df['target'].shift(1).rolling(w).std()\n    df['day_of_week'] = df.index.dayofweek\n    df['month'] = df.index.month\n    df['is_weekend'] = (df.index.dayofweek >= 5).astype(int)\n    return df\n```\n\n## Backtesting Framework\n\nWalk-forward validation: train on [0..t], predict [t+1..t+h], slide forward.","fr":"## Choix de la méthode\n\n| Scénario | Meilleure méthode |\n|----------|-------------|\n| Peu de séries, données propres | Prophet, SARIMA |\n| Nombreuses séries (1000+) | LightGBM avec lag features |\n| Saisonnalité complexe | N-BEATS, TFT |\n| Temps réel, faible latence | Lissage exponentiel simple |\n\n## LightGBM pour les séries temporelles\n\n```python\ndef make_features(df, lags=[1,7,14,28], windows=[7,14,28]):\n    for lag in lags:\n        df[f'lag_{lag}'] = df['target'].shift(lag)\n    for w in windows:\n        df[f'rolling_mean_{w}'] = df['target'].shift(1).rolling(w).mean()\n        df[f'rolling_std_{w}'] = df['target'].shift(1).rolling(w).std()\n    df['day_of_week'] = df.index.dayofweek\n    df['month'] = df.index.month\n    df['is_weekend'] = (df.index.dayofweek >= 5).astype(int)\n    return df\n```\n\n## Framework de backtesting\n\nValidation walk-forward : entraîner sur [0..t], prédire [t+1..t+h], glisser vers l'avant.","ar":"## اختيار الطريقة\n\n| السيناريو | الطريقة الأفضل |\n|----------|-------------|\n| سلاسل قليلة، بيانات نظيفة | Prophet، SARIMA |\n| سلاسل كثيرة (1000+) | LightGBM مع ميزات الإزاحة |\n| موسمية معقدة | N-BEATS، TFT |\n| زمن حقيقي، زمن استجابة منخفض | التنعيم الأسي البسيط |\n\n## LightGBM للسلاسل الزمنية\n\n```python\ndef make_features(df, lags=[1,7,14,28], windows=[7,14,28]):\n    for lag in lags:\n        df[f'lag_{lag}'] = df['target'].shift(lag)\n    for w in windows:\n        df[f'rolling_mean_{w}'] = df['target'].shift(1).rolling(w).mean()\n        df[f'rolling_std_{w}'] = df['target'].shift(1).rolling(w).std()\n    df['day_of_week'] = df.index.dayofweek\n    df['month'] = df.index.month\n    df['is_weekend'] = (df.index.dayofweek >= 5).astype(int)\n    return df\n```\n\n## إطار الاختبار الرجعي\n\nالتحقق walk-forward: درّب على [0..t]، وتنبّأ بـ [t+1..t+h]، ثم انزلق للأمام."},
  "reinforcement-learning-dqn-from-scratch": {"en":"## DQN Core Components\n\n### Replay Buffer\n```python\nclass ReplayBuffer:\n    def __init__(self, capacity=10000):\n        self.buffer = deque(maxlen=capacity)\n    \n    def push(self, state, action, reward, next_state, done):\n        self.buffer.append((state, action, reward, next_state, done))\n    \n    def sample(self, batch_size):\n        return random.sample(self.buffer, batch_size)\n```\n\n### Q-Network\n```python\nclass DQN(nn.Module):\n    def __init__(self, state_dim, action_dim):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(state_dim, 256), nn.ReLU(),\n            nn.Linear(256, 256), nn.ReLU(),\n            nn.Linear(256, action_dim)\n        )\n    def forward(self, x): return self.net(x)\n```\n\n### Training Loop Key Points\n\n- **Target network**: copy weights every 1000 steps (prevents oscillation)\n- **Epsilon decay**: linear from 1.0 to 0.01 over 50K steps\n- **Batch size**: 64, update every 4 steps\n- **Double DQN**: reduces overestimation bias","fr":"## Composants clés du DQN\n\n### Replay buffer\n```python\nclass ReplayBuffer:\n    def __init__(self, capacity=10000):\n        self.buffer = deque(maxlen=capacity)\n    \n    def push(self, state, action, reward, next_state, done):\n        self.buffer.append((state, action, reward, next_state, done))\n    \n    def sample(self, batch_size):\n        return random.sample(self.buffer, batch_size)\n```\n\n### Q-Network\n```python\nclass DQN(nn.Module):\n    def __init__(self, state_dim, action_dim):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(state_dim, 256), nn.ReLU(),\n            nn.Linear(256, 256), nn.ReLU(),\n            nn.Linear(256, action_dim)\n        )\n    def forward(self, x): return self.net(x)\n```\n\n### Points clés de la boucle d'entraînement\n\n- **Réseau cible** : copier les poids tous les 1000 pas (évite les oscillations)\n- **Décroissance d'epsilon** : linéaire de 1.0 à 0.01 sur 50K pas\n- **Taille de batch** : 64, mise à jour tous les 4 pas\n- **Double DQN** : réduit le biais de surestimation","ar":"## المكوّنات الأساسية لـ DQN\n\n### ذاكرة الإعادة (Replay Buffer)\n```python\nclass ReplayBuffer:\n    def __init__(self, capacity=10000):\n        self.buffer = deque(maxlen=capacity)\n    \n    def push(self, state, action, reward, next_state, done):\n        self.buffer.append((state, action, reward, next_state, done))\n    \n    def sample(self, batch_size):\n        return random.sample(self.buffer, batch_size)\n```\n\n### شبكة Q\n```python\nclass DQN(nn.Module):\n    def __init__(self, state_dim, action_dim):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(state_dim, 256), nn.ReLU(),\n            nn.Linear(256, 256), nn.ReLU(),\n            nn.Linear(256, action_dim)\n        )\n    def forward(self, x): return self.net(x)\n```\n\n### نقاط رئيسية في حلقة التدريب\n\n- **الشبكة الهدف**: انسخ الأوزان كل 1000 خطوة (يمنع التذبذب)\n- **تناقص إبسيلون**: خطي من 1.0 إلى 0.01 على مدى 50 ألف خطوة\n- **حجم الدفعة**: 64، التحديث كل 4 خطوات\n- **Double DQN**: يقلّل تحيّز المبالغة في التقدير"},
  "face-recognition-production-insightface": {"en":"## Pipeline\n\n```\nImage → RetinaFace Detection → Landmark Alignment → ArcFace Embedding → Faiss Index\n```\n\n## Code\n\n```python\nfrom insightface.app import FaceAnalysis\nimport faiss\nimport numpy as np\n\napp = FaceAnalysis(providers=['CUDAExecutionProvider'])\napp.prepare(ctx_id=0, det_size=(640, 640))\n\n# Extract embeddings\ndef get_embedding(image):\n    faces = app.get(image)\n    if not faces: return None\n    return faces[0].normed_embedding  # 512-dim L2-normalized\n\n# Build Faiss index\nindex = faiss.IndexFlatIP(512)  # Inner product for cosine sim\nindex.add(np.array(embeddings))\n\n# Search\nD, I = index.search(query_embedding.reshape(1, -1), k=5)\n```\n\n## Threshold Tuning\n\nArcFace cosine similarity: >0.35 = likely same person. Tune on your specific demographic data.","fr":"## Pipeline\n\n```\nImage → RetinaFace Detection → Landmark Alignment → ArcFace Embedding → Faiss Index\n```\n\n## Code\n\n```python\nfrom insightface.app import FaceAnalysis\nimport faiss\nimport numpy as np\n\napp = FaceAnalysis(providers=['CUDAExecutionProvider'])\napp.prepare(ctx_id=0, det_size=(640, 640))\n\n# Extract embeddings\ndef get_embedding(image):\n    faces = app.get(image)\n    if not faces: return None\n    return faces[0].normed_embedding  # 512-dim L2-normalized\n\n# Build Faiss index\nindex = faiss.IndexFlatIP(512)  # Inner product for cosine sim\nindex.add(np.array(embeddings))\n\n# Search\nD, I = index.search(query_embedding.reshape(1, -1), k=5)\n```\n\n## Réglage du seuil\n\nSimilarité cosinus ArcFace : >0.35 = probablement la même personne. À régler sur vos propres données démographiques.","ar":"## خط الأنابيب\n\n```\nImage → RetinaFace Detection → Landmark Alignment → ArcFace Embedding → Faiss Index\n```\n\n## الكود\n\n```python\nfrom insightface.app import FaceAnalysis\nimport faiss\nimport numpy as np\n\napp = FaceAnalysis(providers=['CUDAExecutionProvider'])\napp.prepare(ctx_id=0, det_size=(640, 640))\n\n# Extract embeddings\ndef get_embedding(image):\n    faces = app.get(image)\n    if not faces: return None\n    return faces[0].normed_embedding  # 512-dim L2-normalized\n\n# Build Faiss index\nindex = faiss.IndexFlatIP(512)  # Inner product for cosine sim\nindex.add(np.array(embeddings))\n\n# Search\nD, I = index.search(query_embedding.reshape(1, -1), k=5)\n```\n\n## ضبط العتبة\n\nتشابه جيب التمام لـ ArcFace: >0.35 = على الأرجح الشخص نفسه. اضبطه على بياناتك الديموغرافية الخاصة."},
  "image-classification-efficientnet-guide": {"en":"## Fine-Tuning Strategy\n\n### Phase 1: Train head only (5 epochs)\n```python\nmodel = EfficientNet.from_pretrained('efficientnet-b4')\nfor param in model.parameters():\n    param.requires_grad = False\n# Unfreeze classifier\nfor param in model._fc.parameters():\n    param.requires_grad = True\n```\n\n### Phase 2: Unfreeze top blocks (10 epochs)\n```python\nfor param in model._blocks[-20:].parameters():\n    param.requires_grad = True\n```\n\n### Phase 3: Full fine-tune with low LR (10 epochs)\n```python\noptimizer = AdamW(model.parameters(), lr=1e-5)\n```\n\n## Label Smoothing\n\n```python\ncriterion = nn.CrossEntropyLoss(label_smoothing=0.1)\n```\nReduces overconfidence — typically +1-2% accuracy on small datasets.","fr":"## Stratégie de fine-tuning\n\n### Phase 1 : entraîner uniquement la tête (5 epochs)\n```python\nmodel = EfficientNet.from_pretrained('efficientnet-b4')\nfor param in model.parameters():\n    param.requires_grad = False\n# Unfreeze classifier\nfor param in model._fc.parameters():\n    param.requires_grad = True\n```\n\n### Phase 2 : dégeler les blocs supérieurs (10 epochs)\n```python\nfor param in model._blocks[-20:].parameters():\n    param.requires_grad = True\n```\n\n### Phase 3 : fine-tune complet avec LR faible (10 epochs)\n```python\noptimizer = AdamW(model.parameters(), lr=1e-5)\n```\n\n## Label smoothing\n\n```python\ncriterion = nn.CrossEntropyLoss(label_smoothing=0.1)\n```\nRéduit la surconfiance — généralement +1-2 % d'accuracy sur les petits jeux de données.","ar":"## استراتيجية الضبط الدقيق\n\n### المرحلة 1: تدريب الرأس فقط (5 حقب)\n```python\nmodel = EfficientNet.from_pretrained('efficientnet-b4')\nfor param in model.parameters():\n    param.requires_grad = False\n# Unfreeze classifier\nfor param in model._fc.parameters():\n    param.requires_grad = True\n```\n\n### المرحلة 2: إلغاء تجميد الكتل العليا (10 حقب)\n```python\nfor param in model._blocks[-20:].parameters():\n    param.requires_grad = True\n```\n\n### المرحلة 3: ضبط دقيق كامل بمعدل تعلم منخفض (10 حقب)\n```python\noptimizer = AdamW(model.parameters(), lr=1e-5)\n```\n\n## تنعيم التسميات\n\n```python\ncriterion = nn.CrossEntropyLoss(label_smoothing=0.1)\n```\nيقلّل الثقة المفرطة — عادةً +1-2% دقة على المجموعات الصغيرة."},
  "gan-image-generation-dcgan": {"en":"## Why GANs Fail\n\n1. **Mode collapse**: Generator outputs same image regardless of noise\n2. **Training instability**: G and D loss oscillate without converging\n3. **Vanishing gradients**: Discriminator becomes too strong\n\n## Fix 1: Spectral Normalization\n\n```python\nfrom torch.nn.utils import spectral_norm\n\nself.conv = spectral_norm(nn.Conv2d(in_ch, out_ch, 4, 2, 1))\n```\n\n## Fix 2: Label Smoothing for Discriminator\n\n```python\nreal_labels = torch.ones(B) * 0.9  # not exactly 1.0\nfake_labels = torch.zeros(B) + 0.1  # not exactly 0.0\n```\n\n## Fix 3: Gradient Penalty (WGAN-GP)\n\nPenalizes the gradient norm of the discriminator — most stable GAN variant for real applications.\n\n## Fix 4: Two Time-Scale Update (TTUR)\n\nUse different learning rates: G=0.0001, D=0.0004.","fr":"## Pourquoi les GANs échouent\n\n1. **Effondrement de mode** : le générateur produit la même image quel que soit le bruit\n2. **Instabilité d'entraînement** : les pertes G et D oscillent sans converger\n3. **Gradients qui s'annulent** : le discriminateur devient trop fort\n\n## Correctif 1 : normalisation spectrale\n\n```python\nfrom torch.nn.utils import spectral_norm\n\nself.conv = spectral_norm(nn.Conv2d(in_ch, out_ch, 4, 2, 1))\n```\n\n## Correctif 2 : label smoothing pour le discriminateur\n\n```python\nreal_labels = torch.ones(B) * 0.9  # not exactly 1.0\nfake_labels = torch.zeros(B) + 0.1  # not exactly 0.0\n```\n\n## Correctif 3 : pénalité de gradient (WGAN-GP)\n\nPénalise la norme du gradient du discriminateur — la variante GAN la plus stable pour les applications réelles.\n\n## Correctif 4 : Two Time-Scale Update (TTUR)\n\nUtilisez des taux d'apprentissage différents : G=0.0001, D=0.0004.","ar":"## لماذا تفشل شبكات GAN\n\n1. **انهيار النمط**: يُنتج المولّد الصورة نفسها بغضّ النظر عن الضوضاء\n2. **عدم استقرار التدريب**: تتذبذب خسائر G وD دون تقارب\n3. **تلاشي التدرجات**: يصبح المميِّز قوياً جداً\n\n## الإصلاح 1: التطبيع الطيفي\n\n```python\nfrom torch.nn.utils import spectral_norm\n\nself.conv = spectral_norm(nn.Conv2d(in_ch, out_ch, 4, 2, 1))\n```\n\n## الإصلاح 2: تنعيم التسميات للمميِّز\n\n```python\nreal_labels = torch.ones(B) * 0.9  # not exactly 1.0\nfake_labels = torch.zeros(B) + 0.1  # not exactly 0.0\n```\n\n## الإصلاح 3: عقوبة التدرج (WGAN-GP)\n\nتعاقب معيار تدرج المميِّز — وهي أكثر متغيرات GAN استقراراً للتطبيقات الحقيقية.\n\n## الإصلاح 4: التحديث ثنائي المقياس الزمني (TTUR)\n\nاستخدم معدلات تعلم مختلفة: G=0.0001، D=0.0004."},
  "pandas-performance-optimization": {"en":"## 1. Downcast dtypes (Biggest Win)\n\n```python\ndef reduce_mem_usage(df):\n    for col in df.columns:\n        col_type = df[col].dtype\n        if col_type != object:\n            c_min = df[col].min()\n            c_max = df[col].max()\n            if str(col_type)[:3] == 'int':\n                if c_min > np.iinfo(np.int8).min and c_max < np.iinfo(np.int8).max:\n                    df[col] = df[col].astype(np.int8)\n            else:\n                df[col] = pd.to_numeric(df[col], downcast='float')\n    return df\n```\nTypically saves **50-70% memory**.\n\n## 2. Use Categoricals for Low-Cardinality Strings\n\n```python\ndf['city'] = df['city'].astype('category')  # 10x less memory\n```\n\n## 3. Vectorize Instead of Apply\n\n```python\n# SLOW\ndf['result'] = df.apply(lambda r: r['a'] * r['b'], axis=1)\n# FAST\ndf['result'] = df['a'] * df['b']\n```\n\n## 4-10. query() method, read_parquet over CSV, chunked reading, numba @jit, Dask for 10GB+, PyArrow backend, parallel groupby.","fr":"## 1. Downcaster les dtypes (le plus gros gain)\n\n```python\ndef reduce_mem_usage(df):\n    for col in df.columns:\n        col_type = df[col].dtype\n        if col_type != object:\n            c_min = df[col].min()\n            c_max = df[col].max()\n            if str(col_type)[:3] == 'int':\n                if c_min > np.iinfo(np.int8).min and c_max < np.iinfo(np.int8).max:\n                    df[col] = df[col].astype(np.int8)\n            else:\n                df[col] = pd.to_numeric(df[col], downcast='float')\n    return df\n```\nÉconomise généralement **50-70 % de mémoire**.\n\n## 2. Utiliser des Categoricals pour les chaînes à faible cardinalité\n\n```python\ndf['city'] = df['city'].astype('category')  # 10x less memory\n```\n\n## 3. Vectoriser au lieu d'apply\n\n```python\n# SLOW\ndf['result'] = df.apply(lambda r: r['a'] * r['b'], axis=1)\n# FAST\ndf['result'] = df['a'] * df['b']\n```\n\n## 4-10. méthode query(), read_parquet plutôt que CSV, lecture par chunks, numba @jit, Dask pour 10GB+, backend PyArrow, groupby parallèle.","ar":"## 1. تصغير أنواع البيانات (أكبر مكسب)\n\n```python\ndef reduce_mem_usage(df):\n    for col in df.columns:\n        col_type = df[col].dtype\n        if col_type != object:\n            c_min = df[col].min()\n            c_max = df[col].max()\n            if str(col_type)[:3] == 'int':\n                if c_min > np.iinfo(np.int8).min and c_max < np.iinfo(np.int8).max:\n                    df[col] = df[col].astype(np.int8)\n            else:\n                df[col] = pd.to_numeric(df[col], downcast='float')\n    return df\n```\nيوفّر عادةً **50-70% من الذاكرة**.\n\n## 2. استخدام Categoricals للسلاسل منخفضة التعددية\n\n```python\ndf['city'] = df['city'].astype('category')  # 10x less memory\n```\n\n## 3. التجهيز المتجه بدلاً من apply\n\n```python\n# SLOW\ndf['result'] = df.apply(lambda r: r['a'] * r['b'], axis=1)\n# FAST\ndf['result'] = df['a'] * df['b']\n```\n\n## 4-10. الطريقة query()، وread_parquet بدلاً من CSV، والقراءة على دفعات، وnumba @jit، وDask لأكثر من 10GB، وواجهة PyArrow الخلفية، وgroupby المتوازي."},
  "postgresql-ml-pipeline-design": {"en":"## Feature Store Schema\n\n```sql\nCREATE TABLE features (\n    entity_id   BIGINT,\n    feature_name VARCHAR(100),\n    feature_value FLOAT,\n    computed_at TIMESTAMPTZ,\n    PRIMARY KEY (entity_id, feature_name, computed_at)\n) PARTITION BY RANGE (computed_at);\n```\n\n## Materialized Views for Aggregations\n\n```sql\nCREATE MATERIALIZED VIEW user_stats AS\nSELECT \n    user_id,\n    COUNT(*) as txn_count,\n    AVG(amount) as avg_amount,\n    STDDEV(amount) as std_amount,\n    MAX(txn_at) as last_txn_at\nFROM transactions\nGROUP BY user_id;\n\nCREATE UNIQUE INDEX ON user_stats(user_id);\nREFRESH MATERIALIZED VIEW CONCURRENTLY user_stats;\n```\n\n## Refresh Strategy\n\nSchedule via pg_cron every 15 minutes for near-real-time features.","fr":"## Schéma du feature store\n\n```sql\nCREATE TABLE features (\n    entity_id   BIGINT,\n    feature_name VARCHAR(100),\n    feature_value FLOAT,\n    computed_at TIMESTAMPTZ,\n    PRIMARY KEY (entity_id, feature_name, computed_at)\n) PARTITION BY RANGE (computed_at);\n```\n\n## Vues matérialisées pour les agrégations\n\n```sql\nCREATE MATERIALIZED VIEW user_stats AS\nSELECT \n    user_id,\n    COUNT(*) as txn_count,\n    AVG(amount) as avg_amount,\n    STDDEV(amount) as std_amount,\n    MAX(txn_at) as last_txn_at\nFROM transactions\nGROUP BY user_id;\n\nCREATE UNIQUE INDEX ON user_stats(user_id);\nREFRESH MATERIALIZED VIEW CONCURRENTLY user_stats;\n```\n\n## Stratégie de rafraîchissement\n\nPlanifiez via pg_cron toutes les 15 minutes pour des features en quasi-temps réel.","ar":"## مخطط مخزن الميزات\n\n```sql\nCREATE TABLE features (\n    entity_id   BIGINT,\n    feature_name VARCHAR(100),\n    feature_value FLOAT,\n    computed_at TIMESTAMPTZ,\n    PRIMARY KEY (entity_id, feature_name, computed_at)\n) PARTITION BY RANGE (computed_at);\n```\n\n## المشاهدات المجسّدة للتجميعات\n\n```sql\nCREATE MATERIALIZED VIEW user_stats AS\nSELECT \n    user_id,\n    COUNT(*) as txn_count,\n    AVG(amount) as avg_amount,\n    STDDEV(amount) as std_amount,\n    MAX(txn_at) as last_txn_at\nFROM transactions\nGROUP BY user_id;\n\nCREATE UNIQUE INDEX ON user_stats(user_id);\nREFRESH MATERIALIZED VIEW CONCURRENTLY user_stats;\n```\n\n## استراتيجية التحديث\n\nجدوِل عبر pg_cron كل 15 دقيقة للحصول على ميزات شبه فورية."},
  "neat-neuroevolution-tutorial": {"en":"## What Makes NEAT Special\n\nUnlike standard NNs, NEAT starts with minimal networks and adds complexity only when needed. This prevents bloat and finds efficient solutions.\n\n## Core Concepts\n\n### Speciation\nNetworks are grouped into species based on structural similarity. Each species competes internally, preserving diversity.\n\n### Innovation Numbers\nEvery new structural mutation (new connection/node) gets a unique global innovation number. This allows meaningful crossover between different topologies.\n\n### Fitness Sharing\nFitness is divided by species size to prevent any one species from dominating.\n\n## Python Implementation (neat-python)\n\n```python\nimport neat\n\nconfig = neat.Config(\n    neat.DefaultGenome,\n    neat.DefaultReproduction,\n    neat.DefaultSpeciesSet,\n    neat.DefaultStagnation,\n    'config-feedforward'\n)\n\ndef eval_genomes(genomes, config):\n    for genome_id, genome in genomes:\n        net = neat.nn.FeedForwardNetwork.create(genome, config)\n        genome.fitness = run_game(net)  # your game/simulation\n\npopulation = neat.Population(config)\npopulation.run(eval_genomes, n=1000)\n```","fr":"## Ce qui rend NEAT spécial\n\nContrairement aux réseaux classiques, NEAT démarre avec des réseaux minimaux et n'ajoute de la complexité que lorsque nécessaire. Cela évite le surdimensionnement et trouve des solutions efficaces.\n\n## Concepts clés\n\n### Spéciation\nLes réseaux sont regroupés en espèces selon leur similarité structurelle. Chaque espèce concourt en interne, préservant la diversité.\n\n### Numéros d'innovation\nChaque nouvelle mutation structurelle (nouvelle connexion/nœud) reçoit un numéro d'innovation global unique. Cela permet un croisement pertinent entre topologies différentes.\n\n### Partage de fitness\nLa fitness est divisée par la taille de l'espèce pour empêcher une espèce de dominer.\n\n## Implémentation Python (neat-python)\n\n```python\nimport neat\n\nconfig = neat.Config(\n    neat.DefaultGenome,\n    neat.DefaultReproduction,\n    neat.DefaultSpeciesSet,\n    neat.DefaultStagnation,\n    'config-feedforward'\n)\n\ndef eval_genomes(genomes, config):\n    for genome_id, genome in genomes:\n        net = neat.nn.FeedForwardNetwork.create(genome, config)\n        genome.fitness = run_game(net)  # your game/simulation\n\npopulation = neat.Population(config)\npopulation.run(eval_genomes, n=1000)\n```","ar":"## ما الذي يميّز NEAT\n\nخلافاً للشبكات التقليدية، تبدأ NEAT بشبكات صغرى وتضيف التعقيد فقط عند الحاجة. وهذا يمنع التضخم ويجد حلولاً فعّالة.\n\n## المفاهيم الأساسية\n\n### التخصّص (Speciation)\nتُجمَّع الشبكات في أنواع بناءً على التشابه البنيوي. ويتنافس كل نوع داخلياً، مما يحافظ على التنوّع.\n\n### أرقام الابتكار\nتحصل كل طفرة بنيوية جديدة (اتصال/عقدة جديدة) على رقم ابتكار عالمي فريد. وهذا يتيح تهجيناً ذا معنى بين طوبولوجيات مختلفة.\n\n### مشاركة اللياقة\nتُقسَم اللياقة على حجم النوع لمنع أي نوع من الهيمنة.\n\n## التنفيذ بلغة Python (neat-python)\n\n```python\nimport neat\n\nconfig = neat.Config(\n    neat.DefaultGenome,\n    neat.DefaultReproduction,\n    neat.DefaultSpeciesSet,\n    neat.DefaultStagnation,\n    'config-feedforward'\n)\n\ndef eval_genomes(genomes, config):\n    for genome_id, genome in genomes:\n        net = neat.nn.FeedForwardNetwork.create(genome, config)\n        genome.fitness = run_game(net)  # your game/simulation\n\npopulation = neat.Population(config)\npopulation.run(eval_genomes, n=1000)\n```"},
  "monte-carlo-tree-search-explained": {"en":"## The Four Steps\n\n### 1. Selection\nTraverse the tree from root, choosing children using UCB1:\n```\nUCB1 = win_rate + C * sqrt(ln(parent_visits) / node_visits)\n```\nC is the exploration constant (typically √2).\n\n### 2. Expansion\nIf the node is not terminal, add one or more child nodes.\n\n### 3. Simulation (Rollout)\nPlay random moves until game over. Return win/loss/draw.\n\n### 4. Backpropagation\nUpdate win counts and visit counts all the way up to root.\n\n## Implementation for 2048\n\n```python\nclass MCTSNode:\n    def __init__(self, state, parent=None, action=None):\n        self.state = state\n        self.parent = parent\n        self.action = action\n        self.children = []\n        self.wins = 0\n        self.visits = 0\n    \n    @property\n    def ucb1(self):\n        if self.visits == 0: return float('inf')\n        return self.wins/self.visits + 1.414*np.sqrt(np.log(self.parent.visits)/self.visits)\n```","fr":"## Les quatre étapes\n\n### 1. Sélection\nParcourez l'arbre depuis la racine en choisissant les enfants via UCB1 :\n```\nUCB1 = win_rate + C * sqrt(ln(parent_visits) / node_visits)\n```\nC est la constante d'exploration (typiquement √2).\n\n### 2. Expansion\nSi le nœud n'est pas terminal, ajoutez un ou plusieurs nœuds enfants.\n\n### 3. Simulation (rollout)\nJouez des coups aléatoires jusqu'à la fin de la partie. Retournez victoire/défaite/nul.\n\n### 4. Rétropropagation\nMettez à jour les compteurs de victoires et de visites jusqu'à la racine.\n\n## Implémentation pour 2048\n\n```python\nclass MCTSNode:\n    def __init__(self, state, parent=None, action=None):\n        self.state = state\n        self.parent = parent\n        self.action = action\n        self.children = []\n        self.wins = 0\n        self.visits = 0\n    \n    @property\n    def ucb1(self):\n        if self.visits == 0: return float('inf')\n        return self.wins/self.visits + 1.414*np.sqrt(np.log(self.parent.visits)/self.visits)\n```","ar":"## الخطوات الأربع\n\n### 1. الاختيار\nاجتَز الشجرة من الجذر، مختاراً الأبناء باستخدام UCB1:\n```\nUCB1 = win_rate + C * sqrt(ln(parent_visits) / node_visits)\n```\nC هو ثابت الاستكشاف (عادةً √2).\n\n### 2. التوسّع\nإذا لم تكن العقدة نهائية، أضف عقدة ابن واحدة أو أكثر.\n\n### 3. المحاكاة (Rollout)\nالعب حركات عشوائية حتى انتهاء اللعبة. أعِد فوز/خسارة/تعادل.\n\n### 4. النشر الخلفي\nحدّث عدّادات الفوز والزيارات وصولاً إلى الجذر.\n\n## التنفيذ للعبة 2048\n\n```python\nclass MCTSNode:\n    def __init__(self, state, parent=None, action=None):\n        self.state = state\n        self.parent = parent\n        self.action = action\n        self.children = []\n        self.wins = 0\n        self.visits = 0\n    \n    @property\n    def ucb1(self):\n        if self.visits == 0: return float('inf')\n        return self.wins/self.visits + 1.414*np.sqrt(np.log(self.parent.visits)/self.visits)\n```"},
  "genetic-algorithms-optimization": {"en":"## When to Use Genetic Algorithms\n\n- Combinatorial problems (feature selection, scheduling)\n- Non-differentiable objective functions\n- Multi-modal search spaces\n- When Bayesian optimization is too expensive\n\n## Feature Selection with GA\n\n```python\nfrom deap import base, creator, tools, algorithms\n\n# Genome: binary vector (1=feature included, 0=excluded)\ncreator.create(\"FitnessMax\", base.Fitness, weights=(1.0,))\ncreator.create(\"Individual\", list, fitness=creator.FitnessMax)\n\ndef evaluate(individual):\n    selected_features = [i for i, v in enumerate(individual) if v == 1]\n    if not selected_features: return (0.0,)\n    X_sel = X_train[:, selected_features]\n    score = cross_val_score(model, X_sel, y_train, cv=3).mean()\n    # Penalize using too many features\n    penalty = len(selected_features) / len(individual) * 0.05\n    return (score - penalty,)\n```\n\n## Key Parameters\n\n- Population: 50-200 individuals\n- Mutation rate: 0.01-0.05 per gene\n- Crossover rate: 0.7-0.9\n- Generations: 100-500 (early stopping on plateau)","fr":"## Quand utiliser les algorithmes génétiques\n\n- Problèmes combinatoires (sélection de features, planification)\n- Fonctions objectif non différentiables\n- Espaces de recherche multimodaux\n- Quand l'optimisation bayésienne est trop coûteuse\n\n## Sélection de features avec GA\n\n```python\nfrom deap import base, creator, tools, algorithms\n\n# Genome: binary vector (1=feature included, 0=excluded)\ncreator.create(\"FitnessMax\", base.Fitness, weights=(1.0,))\ncreator.create(\"Individual\", list, fitness=creator.FitnessMax)\n\ndef evaluate(individual):\n    selected_features = [i for i, v in enumerate(individual) if v == 1]\n    if not selected_features: return (0.0,)\n    X_sel = X_train[:, selected_features]\n    score = cross_val_score(model, X_sel, y_train, cv=3).mean()\n    # Penalize using too many features\n    penalty = len(selected_features) / len(individual) * 0.05\n    return (score - penalty,)\n```\n\n## Paramètres clés\n\n- Population : 50-200 individus\n- Taux de mutation : 0.01-0.05 par gène\n- Taux de croisement : 0.7-0.9\n- Générations : 100-500 (arrêt anticipé sur plateau)","ar":"## متى تستخدم الخوارزميات الجينية\n\n- المشكلات التوافقية (اختيار الميزات، الجدولة)\n- دوال الهدف غير القابلة للاشتقاق\n- فضاءات البحث متعددة القمم\n- عندما يكون التحسين البايزي مكلفاً جداً\n\n## اختيار الميزات بالخوارزمية الجينية\n\n```python\nfrom deap import base, creator, tools, algorithms\n\n# Genome: binary vector (1=feature included, 0=excluded)\ncreator.create(\"FitnessMax\", base.Fitness, weights=(1.0,))\ncreator.create(\"Individual\", list, fitness=creator.FitnessMax)\n\ndef evaluate(individual):\n    selected_features = [i for i, v in enumerate(individual) if v == 1]\n    if not selected_features: return (0.0,)\n    X_sel = X_train[:, selected_features]\n    score = cross_val_score(model, X_sel, y_train, cv=3).mean()\n    # Penalize using too many features\n    penalty = len(selected_features) / len(individual) * 0.05\n    return (score - penalty,)\n```\n\n## المعاملات الرئيسية\n\n- عدد السكان: 50-200 فرد\n- معدل الطفرة: 0.01-0.05 لكل جين\n- معدل التهجين: 0.7-0.9\n- الأجيال: 100-500 (إيقاف مبكر عند الثبات)"},
  "llm-local-deployment-ollama": {"en":"## Why Local LLMs\n\n1. **Data privacy** — sensitive business data never leaves your server\n2. **Cost** — zero per-token cost after hardware investment\n3. **Latency** — sub-100ms on good hardware\n4. **Customization** — fine-tune on proprietary data\n\n## Setup\n\n```bash\n# Install\ncurl -fsSL https://ollama.ai/install.sh | sh\n\n# Pull model\nollama pull llama3.1:8b\nollama pull mistral:7b\n\n# Serve (exposes REST API on port 11434)\nollama serve\n```\n\n## Python Integration\n\n```python\nimport requests\n\ndef chat(prompt, model='llama3.1:8b'):\n    response = requests.post(\n        'http://localhost:11434/api/chat',\n        json={\n            'model': model,\n            'messages': [{'role': 'user', 'content': prompt}],\n            'stream': False\n        }\n    )\n    return response.json()['message']['content']\n```\n\n## Model Recommendations\n\n| Use Case | Model | VRAM |\n|----------|-------|------|\n| Fast responses | Mistral 7B | 8GB |\n| Reasoning | Llama 3.1 8B | 8GB |\n| Complex tasks | Llama 3.1 70B | 48GB |\n| Code | Qwen2.5-Coder 7B | 8GB |","fr":"## Pourquoi des LLMs locaux\n\n1. **Confidentialité des données** — les données métier sensibles ne quittent jamais votre serveur\n2. **Coût** — coût par token nul après l'investissement matériel\n3. **Latence** — sous les 100ms sur du bon matériel\n4. **Personnalisation** — fine-tuning sur données propriétaires\n\n## Installation\n\n```bash\n# Install\ncurl -fsSL https://ollama.ai/install.sh | sh\n\n# Pull model\nollama pull llama3.1:8b\nollama pull mistral:7b\n\n# Serve (exposes REST API on port 11434)\nollama serve\n```\n\n## Intégration Python\n\n```python\nimport requests\n\ndef chat(prompt, model='llama3.1:8b'):\n    response = requests.post(\n        'http://localhost:11434/api/chat',\n        json={\n            'model': model,\n            'messages': [{'role': 'user', 'content': prompt}],\n            'stream': False\n        }\n    )\n    return response.json()['message']['content']\n```\n\n## Recommandations de modèles\n\n| Cas d'usage | Modèle | VRAM |\n|----------|-------|------|\n| Réponses rapides | Mistral 7B | 8GB |\n| Raisonnement | Llama 3.1 8B | 8GB |\n| Tâches complexes | Llama 3.1 70B | 48GB |\n| Code | Qwen2.5-Coder 7B | 8GB |","ar":"## لماذا النماذج المحلية\n\n1. **خصوصية البيانات** — البيانات التجارية الحساسة لا تغادر خادمك أبداً\n2. **التكلفة** — تكلفة صفرية لكل رمز بعد استثمار العتاد\n3. **الكمون** — أقل من 100 مللي ثانية على عتاد جيد\n4. **التخصيص** — ضبط دقيق على بيانات خاصة\n\n## الإعداد\n\n```bash\n# Install\ncurl -fsSL https://ollama.ai/install.sh | sh\n\n# Pull model\nollama pull llama3.1:8b\nollama pull mistral:7b\n\n# Serve (exposes REST API on port 11434)\nollama serve\n```\n\n## تكامل Python\n\n```python\nimport requests\n\ndef chat(prompt, model='llama3.1:8b'):\n    response = requests.post(\n        'http://localhost:11434/api/chat',\n        json={\n            'model': model,\n            'messages': [{'role': 'user', 'content': prompt}],\n            'stream': False\n        }\n    )\n    return response.json()['message']['content']\n```\n\n## توصيات النماذج\n\n| حالة الاستخدام | النموذج | VRAM |\n|----------|-------|------|\n| ردود سريعة | Mistral 7B | 8GB |\n| الاستدلال | Llama 3.1 8B | 8GB |\n| مهام معقدة | Llama 3.1 70B | 48GB |\n| البرمجة | Qwen2.5-Coder 7B | 8GB |"},
  "vector-databases-comparison-2025": {"en":"## Summary Table\n\n| DB | Hosting | Best For | Free Tier |\n|----|---------|----------|----------|\n| Pinecone | Cloud | Production RAG | Yes (1 index) |\n| Weaviate | Self/Cloud | Hybrid search | Yes |\n| Qdrant | Self/Cloud | High performance | Yes |\n| Chroma | Self | Development/Local | Yes |\n| Faiss | Self | Research/offline | Yes |\n\n## My Recommendation\n\n- **Development/prototyping**: Chroma (zero setup)\n- **Production RAG**: Pinecone (managed, simple SDK)\n- **Self-hosted production**: Qdrant (fastest, Docker-friendly)\n- **Hybrid search**: Weaviate (BM25 + vector built-in)\n\n## Qdrant Quick Start\n\n```python\nfrom qdrant_client import QdrantClient\n\nclient = QdrantClient(\":memory:\")\nclient.create_collection('docs', vectors_config=VectorParams(size=768, distance=Distance.COSINE))\nclient.upsert('docs', points=[PointStruct(id=1, vector=embedding, payload={'text': '...'})])\nresults = client.search('docs', query_vector=query_embedding, limit=5)\n```","fr":"## Tableau récapitulatif\n\n| BD | Hébergement | Idéal pour | Offre gratuite |\n|----|---------|----------|----------|\n| Pinecone | Cloud | RAG en production | Oui (1 index) |\n| Weaviate | Self/Cloud | Recherche hybride | Oui |\n| Qdrant | Self/Cloud | Haute performance | Oui |\n| Chroma | Self | Développement/Local | Oui |\n| Faiss | Self | Recherche/hors ligne | Oui |\n\n## Ma recommandation\n\n- **Développement/prototypage** : Chroma (zéro configuration)\n- **RAG en production** : Pinecone (managé, SDK simple)\n- **Production auto-hébergée** : Qdrant (le plus rapide, compatible Docker)\n- **Recherche hybride** : Weaviate (BM25 + vecteur intégré)\n\n## Démarrage rapide Qdrant\n\n```python\nfrom qdrant_client import QdrantClient\n\nclient = QdrantClient(\":memory:\")\nclient.create_collection('docs', vectors_config=VectorParams(size=768, distance=Distance.COSINE))\nclient.upsert('docs', points=[PointStruct(id=1, vector=embedding, payload={'text': '...'})])\nresults = client.search('docs', query_vector=query_embedding, limit=5)\n```","ar":"## جدول ملخّص\n\n| القاعدة | الاستضافة | الأفضل لـ | الباقة المجانية |\n|----|---------|----------|----------|\n| Pinecone | سحابي | RAG الإنتاجي | نعم (فهرس واحد) |\n| Weaviate | ذاتي/سحابي | البحث الهجين | نعم |\n| Qdrant | ذاتي/سحابي | الأداء العالي | نعم |\n| Chroma | ذاتي | التطوير/المحلي | نعم |\n| Faiss | ذاتي | البحث/دون اتصال | نعم |\n\n## توصيتي\n\n- **التطوير/النماذج الأولية**: Chroma (دون أي إعداد)\n- **RAG الإنتاجي**: Pinecone (مُدار، SDK بسيط)\n- **الإنتاج المستضاف ذاتياً**: Qdrant (الأسرع، متوافق مع Docker)\n- **البحث الهجين**: Weaviate (BM25 + متجه مدمج)\n\n## بداية سريعة مع Qdrant\n\n```python\nfrom qdrant_client import QdrantClient\n\nclient = QdrantClient(\":memory:\")\nclient.create_collection('docs', vectors_config=VectorParams(size=768, distance=Distance.COSINE))\nclient.upsert('docs', points=[PointStruct(id=1, vector=embedding, payload={'text': '...'})])\nresults = client.search('docs', query_vector=query_embedding, limit=5)\n```"},
  "docker-ml-workflows-best-practices": {"en":"## Multi-Stage Dockerfile\n\n```dockerfile\n# Stage 1: Build environment\nFROM python:3.11-slim as builder\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir --user -r requirements.txt\n\n# Stage 2: Runtime\nFROM python:3.11-slim\nWORKDIR /app\nCOPY --from=builder /root/.local /root/.local\nCOPY . .\nENV PATH=/root/.local/bin:$PATH\n\nCMD [\"uvicorn\", \"app:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\n**Result: 2.1GB → 620MB image**\n\n## GPU Support\n\n```dockerfile\nFROM nvidia/cuda:12.1-cudnn8-runtime-ubuntu22.04\n```\n\n## Model Caching Strategy\n\nBake the model into the image for zero cold-start:\n```dockerfile\nRUN python -c \"from transformers import AutoModel; AutoModel.from_pretrained('bert-base-multilingual-cased')\"\n```","fr":"## Dockerfile multi-étapes\n\n```dockerfile\n# Stage 1: Build environment\nFROM python:3.11-slim as builder\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir --user -r requirements.txt\n\n# Stage 2: Runtime\nFROM python:3.11-slim\nWORKDIR /app\nCOPY --from=builder /root/.local /root/.local\nCOPY . .\nENV PATH=/root/.local/bin:$PATH\n\nCMD [\"uvicorn\", \"app:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\n**Résultat : image 2.1GB → 620MB**\n\n## Support GPU\n\n```dockerfile\nFROM nvidia/cuda:12.1-cudnn8-runtime-ubuntu22.04\n```\n\n## Stratégie de cache du modèle\n\nIntégrez le modèle dans l'image pour un démarrage à froid nul :\n```dockerfile\nRUN python -c \"from transformers import AutoModel; AutoModel.from_pretrained('bert-base-multilingual-cased')\"\n```","ar":"## Dockerfile متعدد المراحل\n\n```dockerfile\n# Stage 1: Build environment\nFROM python:3.11-slim as builder\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir --user -r requirements.txt\n\n# Stage 2: Runtime\nFROM python:3.11-slim\nWORKDIR /app\nCOPY --from=builder /root/.local /root/.local\nCOPY . .\nENV PATH=/root/.local/bin:$PATH\n\nCMD [\"uvicorn\", \"app:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\n**النتيجة: صورة 2.1GB → 620MB**\n\n## دعم GPU\n\n```dockerfile\nFROM nvidia/cuda:12.1-cudnn8-runtime-ubuntu22.04\n```\n\n## استراتيجية تخزين النموذج مؤقتاً\n\nاخبز النموذج داخل الصورة لبدء بارد صفري:\n```dockerfile\nRUN python -c \"from transformers import AutoModel; AutoModel.from_pretrained('bert-base-multilingual-cased')\"\n```"},
  "sklearn-pipeline-best-practices": {"en":"## The Problem Without Pipelines\n\n```python\n# WRONG: Fit scaler on all data (data leakage!)\nscaler.fit(X)  # should only fit on train set\nX_scaled = scaler.transform(X)\ncross_val_score(model, X_scaled, y)  # LEAKS validation data into scaler\n```\n\n## The Right Way\n\n```python\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.impute import SimpleImputer\n\npipe = Pipeline([\n    ('imputer', SimpleImputer(strategy='median')),\n    ('scaler', StandardScaler()),\n    ('model', LGBMClassifier()),\n])\n\n# Now CV is correct — scaler only fits on train fold\ncross_val_score(pipe, X, y, cv=5)  # CORRECT\n```\n\n## Custom Transformer\n\n```python\nclass LogTransformer(BaseEstimator, TransformerMixin):\n    def fit(self, X, y=None): return self\n    def transform(self, X):\n        return np.log1p(np.abs(X))\n```","fr":"## Le problème sans pipelines\n\n```python\n# WRONG: Fit scaler on all data (data leakage!)\nscaler.fit(X)  # should only fit on train set\nX_scaled = scaler.transform(X)\ncross_val_score(model, X_scaled, y)  # LEAKS validation data into scaler\n```\n\n## La bonne approche\n\n```python\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.impute import SimpleImputer\n\npipe = Pipeline([\n    ('imputer', SimpleImputer(strategy='median')),\n    ('scaler', StandardScaler()),\n    ('model', LGBMClassifier()),\n])\n\n# Now CV is correct — scaler only fits on train fold\ncross_val_score(pipe, X, y, cv=5)  # CORRECT\n```\n\n## Transformeur personnalisé\n\n```python\nclass LogTransformer(BaseEstimator, TransformerMixin):\n    def fit(self, X, y=None): return self\n    def transform(self, X):\n        return np.log1p(np.abs(X))\n```","ar":"## المشكلة بدون خطوط الأنابيب\n\n```python\n# WRONG: Fit scaler on all data (data leakage!)\nscaler.fit(X)  # should only fit on train set\nX_scaled = scaler.transform(X)\ncross_val_score(model, X_scaled, y)  # LEAKS validation data into scaler\n```\n\n## الطريقة الصحيحة\n\n```python\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.impute import SimpleImputer\n\npipe = Pipeline([\n    ('imputer', SimpleImputer(strategy='median')),\n    ('scaler', StandardScaler()),\n    ('model', LGBMClassifier()),\n])\n\n# Now CV is correct — scaler only fits on train fold\ncross_val_score(pipe, X, y, cv=5)  # CORRECT\n```\n\n## محوّل مخصص\n\n```python\nclass LogTransformer(BaseEstimator, TransformerMixin):\n    def fit(self, X, y=None): return self\n    def transform(self, X):\n        return np.log1p(np.abs(X))\n```"},
  "embedding-models-comparison": {"en":"## MTEB Benchmark Results (2025)\n\n| Model | Avg Score | Dim | Cost |\n|-------|-----------|-----|------|\n| text-embedding-3-large | 64.6 | 3072 | $0.13/M tokens |\n| Cohere embed-v3 | 64.5 | 1024 | $0.10/M tokens |\n| BGE-M3 | 63.8 | 1024 | Free |\n| E5-mistral-7b | 66.6 | 4096 | Free |\n| Jina-embeddings-v3 | 65.2 | 1024 | Free |\n\n## For Multilingual (AR/FR/EN)\n\nBGE-M3 and Jina-v3 have the best multilingual coverage at zero cost.\n\n## My Stack\n\n```python\n# Free, local, multilingual\nfrom sentence_transformers import SentenceTransformer\nmodel = SentenceTransformer('BAAI/bge-m3')\nembeddings = model.encode(texts, normalize_embeddings=True)\n```","fr":"## Résultats du benchmark MTEB (2025)\n\n| Modèle | Score moyen | Dim | Coût |\n|-------|-----------|-----|------|\n| text-embedding-3-large | 64.6 | 3072 | $0.13/M tokens |\n| Cohere embed-v3 | 64.5 | 1024 | $0.10/M tokens |\n| BGE-M3 | 63.8 | 1024 | Gratuit |\n| E5-mistral-7b | 66.6 | 4096 | Gratuit |\n| Jina-embeddings-v3 | 65.2 | 1024 | Gratuit |\n\n## Pour le multilingue (AR/FR/EN)\n\nBGE-M3 et Jina-v3 offrent la meilleure couverture multilingue à coût nul.\n\n## Ma stack\n\n```python\n# Free, local, multilingual\nfrom sentence_transformers import SentenceTransformer\nmodel = SentenceTransformer('BAAI/bge-m3')\nembeddings = model.encode(texts, normalize_embeddings=True)\n```","ar":"## نتائج معيار MTEB (2025)\n\n| النموذج | متوسط الدرجة | الأبعاد | التكلفة |\n|-------|-----------|-----|------|\n| text-embedding-3-large | 64.6 | 3072 | $0.13/M tokens |\n| Cohere embed-v3 | 64.5 | 1024 | $0.10/M tokens |\n| BGE-M3 | 63.8 | 1024 | مجاني |\n| E5-mistral-7b | 66.6 | 4096 | مجاني |\n| Jina-embeddings-v3 | 65.2 | 1024 | مجاني |\n\n## للغات المتعددة (AR/FR/EN)\n\nيوفّر BGE-M3 وJina-v3 أفضل تغطية متعددة اللغات بتكلفة صفرية.\n\n## مكدّسي\n\n```python\n# Free, local, multilingual\nfrom sentence_transformers import SentenceTransformer\nmodel = SentenceTransformer('BAAI/bge-m3')\nembeddings = model.encode(texts, normalize_embeddings=True)\n```"},
  "anomaly-detection-autoencoder": {"en":"## Why Autoencoders for Anomaly Detection\n\nYou only need normal data to train. The autoencoder learns to reconstruct normal patterns well. Anomalies have high reconstruction error.\n\n## Architecture\n\n```python\nclass Autoencoder(nn.Module):\n    def __init__(self, input_dim, latent_dim=32):\n        super().__init__()\n        self.encoder = nn.Sequential(\n            nn.Linear(input_dim, 128), nn.ReLU(),\n            nn.Linear(128, 64), nn.ReLU(),\n            nn.Linear(64, latent_dim)\n        )\n        self.decoder = nn.Sequential(\n            nn.Linear(latent_dim, 64), nn.ReLU(),\n            nn.Linear(64, 128), nn.ReLU(),\n            nn.Linear(128, input_dim)\n        )\n    \n    def forward(self, x):\n        z = self.encoder(x)\n        return self.decoder(z)\n```\n\n## Threshold Setting\n\n```python\n# Set threshold at 99th percentile of train reconstruction error\nrecon_errors = ((X_train - model(X_train)) ** 2).mean(dim=1)\nthreshold = np.percentile(recon_errors.numpy(), 99)\n```","fr":"## Pourquoi les autoencodeurs pour la détection d'anomalies\n\nVous n'avez besoin que de données normales pour l'entraînement. L'autoencodeur apprend à bien reconstruire les motifs normaux. Les anomalies ont une erreur de reconstruction élevée.\n\n## Architecture\n\n```python\nclass Autoencoder(nn.Module):\n    def __init__(self, input_dim, latent_dim=32):\n        super().__init__()\n        self.encoder = nn.Sequential(\n            nn.Linear(input_dim, 128), nn.ReLU(),\n            nn.Linear(128, 64), nn.ReLU(),\n            nn.Linear(64, latent_dim)\n        )\n        self.decoder = nn.Sequential(\n            nn.Linear(latent_dim, 64), nn.ReLU(),\n            nn.Linear(64, 128), nn.ReLU(),\n            nn.Linear(128, input_dim)\n        )\n    \n    def forward(self, x):\n        z = self.encoder(x)\n        return self.decoder(z)\n```\n\n## Réglage du seuil\n\n```python\n# Set threshold at 99th percentile of train reconstruction error\nrecon_errors = ((X_train - model(X_train)) ** 2).mean(dim=1)\nthreshold = np.percentile(recon_errors.numpy(), 99)\n```","ar":"## لماذا المشفرات التلقائية للكشف عن الشذوذ\n\nتحتاج فقط إلى بيانات طبيعية للتدريب. يتعلّم المشفّر التلقائي إعادة بناء الأنماط الطبيعية جيداً. أما الحالات الشاذة فيكون خطأ إعادة بنائها مرتفعاً.\n\n## المعمارية\n\n```python\nclass Autoencoder(nn.Module):\n    def __init__(self, input_dim, latent_dim=32):\n        super().__init__()\n        self.encoder = nn.Sequential(\n            nn.Linear(input_dim, 128), nn.ReLU(),\n            nn.Linear(128, 64), nn.ReLU(),\n            nn.Linear(64, latent_dim)\n        )\n        self.decoder = nn.Sequential(\n            nn.Linear(latent_dim, 64), nn.ReLU(),\n            nn.Linear(64, 128), nn.ReLU(),\n            nn.Linear(128, input_dim)\n        )\n    \n    def forward(self, x):\n        z = self.encoder(x)\n        return self.decoder(z)\n```\n\n## ضبط العتبة\n\n```python\n# Set threshold at 99th percentile of train reconstruction error\nrecon_errors = ((X_train - model(X_train)) ** 2).mean(dim=1)\nthreshold = np.percentile(recon_errors.numpy(), 99)\n```"},
  "gpu-training-optimization": {"en":"## Step 1: Diagnose the Bottleneck\n\n```bash\n# GPU utilization — should be 90%+\nnvidia-smi -l 1\n\n# If low GPU util → CPU/DataLoader bottleneck\n```\n\n## Fix DataLoader Bottleneck\n\n```python\nloader = DataLoader(\n    dataset,\n    batch_size=64,\n    num_workers=min(os.cpu_count(), 8),  # key\n    pin_memory=True,                       # key\n    persistent_workers=True,               # key\n    prefetch_factor=2\n)\n```\n\n## CUDA Memory Management\n\n```python\n# Clear cache between experiments\ntorch.cuda.empty_cache()\n\n# Monitor\nprint(f'Allocated: {torch.cuda.memory_allocated()/1e9:.1f}GB')\nprint(f'Reserved:  {torch.cuda.memory_reserved()/1e9:.1f}GB')\n```\n\n## Gradient Accumulation (for large batches on limited VRAM)\n\n```python\nfor i, (x, y) in enumerate(loader):\n    loss = model(x) / accumulation_steps\n    loss.backward()\n    if (i+1) % accumulation_steps == 0:\n        optimizer.step()\n        optimizer.zero_grad()\n```","fr":"## Étape 1 : diagnostiquer le goulot d'étranglement\n\n```bash\n# GPU utilization — should be 90%+\nnvidia-smi -l 1\n\n# If low GPU util → CPU/DataLoader bottleneck\n```\n\n## Corriger le goulot du DataLoader\n\n```python\nloader = DataLoader(\n    dataset,\n    batch_size=64,\n    num_workers=min(os.cpu_count(), 8),  # key\n    pin_memory=True,                       # key\n    persistent_workers=True,               # key\n    prefetch_factor=2\n)\n```\n\n## Gestion de la mémoire CUDA\n\n```python\n# Clear cache between experiments\ntorch.cuda.empty_cache()\n\n# Monitor\nprint(f'Allocated: {torch.cuda.memory_allocated()/1e9:.1f}GB')\nprint(f'Reserved:  {torch.cuda.memory_reserved()/1e9:.1f}GB')\n```\n\n## Accumulation de gradient (pour de grands batches sur VRAM limitée)\n\n```python\nfor i, (x, y) in enumerate(loader):\n    loss = model(x) / accumulation_steps\n    loss.backward()\n    if (i+1) % accumulation_steps == 0:\n        optimizer.step()\n        optimizer.zero_grad()\n```","ar":"## الخطوة 1: تشخيص الاختناق\n\n```bash\n# GPU utilization — should be 90%+\nnvidia-smi -l 1\n\n# If low GPU util → CPU/DataLoader bottleneck\n```\n\n## إصلاح اختناق DataLoader\n\n```python\nloader = DataLoader(\n    dataset,\n    batch_size=64,\n    num_workers=min(os.cpu_count(), 8),  # key\n    pin_memory=True,                       # key\n    persistent_workers=True,               # key\n    prefetch_factor=2\n)\n```\n\n## إدارة ذاكرة CUDA\n\n```python\n# Clear cache between experiments\ntorch.cuda.empty_cache()\n\n# Monitor\nprint(f'Allocated: {torch.cuda.memory_allocated()/1e9:.1f}GB')\nprint(f'Reserved:  {torch.cuda.memory_reserved()/1e9:.1f}GB')\n```\n\n## تراكم التدرج (للدفعات الكبيرة على VRAM محدودة)\n\n```python\nfor i, (x, y) in enumerate(loader):\n    loss = model(x) / accumulation_steps\n    loss.backward()\n    if (i+1) % accumulation_steps == 0:\n        optimizer.step()\n        optimizer.zero_grad()\n```"},
  "sentiment-analysis-production-arabic": {"en":"## Dataset Curation\n\nI used 3 sources:\n1. ASTD (Arabic Sentiment Twitter Dataset) — 10K tweets\n2. ArSAS (Arabic Sentiment Analysis) — 21K MSA + dialectal\n3. Scraped Moroccan Darija reviews from Jumia MA\n\n## Preprocessing\n\n```python\ndef preprocess_ar(text):\n    text = re.sub(r'[\\u064B-\\u065F\\u0670]', '', text)  # Remove diacritics\n    text = re.sub(r'[أإآ]', 'ا', text)  # Normalize alef\n    text = re.sub(r'ة', 'ه', text)  # Normalize teh marbuta\n    text = re.sub(r'[^\\w\\s]', ' ', text)  # Remove punctuation\n    text = re.sub(r'\\s+', ' ', text).strip()\n    return text\n```\n\n## Results Comparison\n\n| Model | Accuracy | F1 |\n|-------|---------|----|\n| TF-IDF + SVM | 76.2% | 0.74 |\n| FastText | 81.5% | 0.80 |\n| AraBERT v0.2 | **88.1%** | **0.87** |\n| CAMeL-BERT | 86.7% | 0.85 |\n\nAraBERT wins but is 20x slower. Use FastText if latency matters.","fr":"## Curation du dataset\n\nJ'ai utilisé 3 sources :\n1. ASTD (Arabic Sentiment Twitter Dataset) — 10K tweets\n2. ArSAS (Arabic Sentiment Analysis) — 21K MSA + dialectal\n3. Avis en darija marocain scrapés depuis Jumia MA\n\n## Prétraitement\n\n```python\ndef preprocess_ar(text):\n    text = re.sub(r'[\\u064B-\\u065F\\u0670]', '', text)  # Remove diacritics\n    text = re.sub(r'[أإآ]', 'ا', text)  # Normalize alef\n    text = re.sub(r'ة', 'ه', text)  # Normalize teh marbuta\n    text = re.sub(r'[^\\w\\s]', ' ', text)  # Remove punctuation\n    text = re.sub(r'\\s+', ' ', text).strip()\n    return text\n```\n\n## Comparaison des résultats\n\n| Modèle | Accuracy | F1 |\n|-------|---------|----|\n| TF-IDF + SVM | 76.2% | 0.74 |\n| FastText | 81.5% | 0.80 |\n| AraBERT v0.2 | **88.1%** | **0.87** |\n| CAMeL-BERT | 86.7% | 0.85 |\n\nAraBERT gagne mais est 20x plus lent. Utilisez FastText si la latence compte.","ar":"## تنظيم مجموعة البيانات\n\nاستخدمت 3 مصادر:\n1. ASTD (مجموعة تغريدات المشاعر العربية) — 10 آلاف تغريدة\n2. ArSAS (تحليل المشاعر العربية) — 21 ألف فصحى + لهجات\n3. مراجعات بالدارجة المغربية مستخرجة من Jumia MA\n\n## المعالجة المسبقة\n\n```python\ndef preprocess_ar(text):\n    text = re.sub(r'[\\u064B-\\u065F\\u0670]', '', text)  # Remove diacritics\n    text = re.sub(r'[أإآ]', 'ا', text)  # Normalize alef\n    text = re.sub(r'ة', 'ه', text)  # Normalize teh marbuta\n    text = re.sub(r'[^\\w\\s]', ' ', text)  # Remove punctuation\n    text = re.sub(r'\\s+', ' ', text).strip()\n    return text\n```\n\n## مقارنة النتائج\n\n| النموذج | الدقة | F1 |\n|-------|---------|----|\n| TF-IDF + SVM | 76.2% | 0.74 |\n| FastText | 81.5% | 0.80 |\n| AraBERT v0.2 | **88.1%** | **0.87** |\n| CAMeL-BERT | 86.7% | 0.85 |\n\nيتفوّق AraBERT لكنه أبطأ بـ 20 مرة. استخدم FastText إذا كان الكمون مهماً."},
  "data-augmentation-small-datasets": {"en":"## Augmentation Hierarchy (Most to Least Impactful)\n\n### Tier 1: Always Do This\n- Random horizontal/vertical flip\n- Random rotation (±15°)\n- Random crop and resize\n- Color jitter (brightness, contrast, saturation)\n\n### Tier 2: Usually Helps\n- Mixup: blend two images and their labels\n```python\nlam = np.random.beta(0.2, 0.2)\nx_mix = lam * x1 + (1-lam) * x2\ny_mix = lam * y1 + (1-lam) * y2\n```\n\n- CutMix: paste a patch from one image to another\n\n### Tier 3: For Very Small Datasets (<200 samples)\n- Elastic transformations (for medical images)\n- Grid distortion\n- Test-time augmentation (TTA) — ensemble 8 augmented versions at inference\n\n### Tier 4: Synthetic Data\n- Train a GAN or use Stable Diffusion to generate additional training samples\n- Works well for domain-specific rare classes","fr":"## Hiérarchie d'augmentation (du plus au moins impactant)\n\n### Niveau 1 : à toujours faire\n- Retournement horizontal/vertical aléatoire\n- Rotation aléatoire (±15°)\n- Crop et redimensionnement aléatoires\n- Color jitter (luminosité, contraste, saturation)\n\n### Niveau 2 : aide généralement\n- Mixup : mélanger deux images et leurs étiquettes\n```python\nlam = np.random.beta(0.2, 0.2)\nx_mix = lam * x1 + (1-lam) * x2\ny_mix = lam * y1 + (1-lam) * y2\n```\n\n- CutMix : coller un patch d'une image sur une autre\n\n### Niveau 3 : pour les très petits jeux de données (<200 exemples)\n- Transformations élastiques (pour les images médicales)\n- Distorsion en grille\n- Augmentation au moment du test (TTA) — assembler 8 versions augmentées à l'inférence\n\n### Niveau 4 : données synthétiques\n- Entraîner un GAN ou utiliser Stable Diffusion pour générer des échantillons supplémentaires\n- Fonctionne bien pour les classes rares spécifiques à un domaine","ar":"## هرمية التوسيع (من الأكثر تأثيراً إلى الأقل)\n\n### المستوى 1: افعل هذا دائماً\n- انعكاس أفقي/رأسي عشوائي\n- تدوير عشوائي (±15°)\n- اقتصاص وتغيير حجم عشوائي\n- اهتزاز لوني (السطوع، التباين، التشبّع)\n\n### المستوى 2: يساعد عادةً\n- Mixup: مزج صورتين وتسمياتهما\n```python\nlam = np.random.beta(0.2, 0.2)\nx_mix = lam * x1 + (1-lam) * x2\ny_mix = lam * y1 + (1-lam) * y2\n```\n\n- CutMix: لصق رقعة من صورة إلى أخرى\n\n### المستوى 3: لمجموعات البيانات الصغيرة جداً (<200 عينة)\n- التحويلات المرنة (للصور الطبية)\n- تشويه الشبكة\n- التوسيع وقت الاختبار (TTA) — تجميع 8 نسخ موسّعة عند الاستدلال\n\n### المستوى 4: البيانات الاصطناعية\n- درّب GAN أو استخدم Stable Diffusion لتوليد عينات تدريب إضافية\n- يعمل جيداً للفئات النادرة الخاصة بمجال معيّن"},
  "ml-system-design-interview": {"en":"## The 6-Step Framework\n\n### 1. Problem Framing (5 min)\n- What is the exact business objective?\n- What does success look like (KPI)?\n- Is this supervised? Online or batch?\n\n### 2. Data Strategy (5 min)\n- What data exists? Volume, freshness, quality?\n- How to handle missing labels?\n- Privacy/compliance constraints?\n\n### 3. Feature Engineering (10 min)\n- Raw features available\n- Computed/aggregated features\n- Embedding features (text, images)\n\n### 4. Modeling (10 min)\n- Baseline (rule-based)\n- ML approach (gradient boosting / NN)\n- Evaluation metric and why\n\n### 5. Serving (10 min)\n- Batch or real-time?\n- Latency requirements\n- Feature store design\n\n### 6. Monitoring (5 min)\n- Data drift, concept drift\n- A/B testing plan\n- Retraining trigger","fr":"## Le framework en 6 étapes\n\n### 1. Cadrage du problème (5 min)\n- Quel est l'objectif métier exact ?\n- À quoi ressemble le succès (KPI) ?\n- Est-ce supervisé ? En ligne ou par lots ?\n\n### 2. Stratégie de données (5 min)\n- Quelles données existent ? Volume, fraîcheur, qualité ?\n- Comment gérer les étiquettes manquantes ?\n- Contraintes de confidentialité/conformité ?\n\n### 3. Feature engineering (10 min)\n- Caractéristiques brutes disponibles\n- Caractéristiques calculées/agrégées\n- Caractéristiques d'embedding (texte, images)\n\n### 4. Modélisation (10 min)\n- Baseline (à base de règles)\n- Approche ML (gradient boosting / NN)\n- Métrique d'évaluation et pourquoi\n\n### 5. Serving (10 min)\n- Par lots ou en temps réel ?\n- Exigences de latence\n- Conception du feature store\n\n### 6. Monitoring (5 min)\n- Dérive des données, dérive du concept\n- Plan de test A/B\n- Déclencheur de réentraînement","ar":"## إطار العمل من 6 خطوات\n\n### 1. صياغة المشكلة (5 دقائق)\n- ما الهدف التجاري الدقيق؟\n- كيف يبدو النجاح (KPI)؟\n- هل هو مُشرف؟ فوري أم على دفعات؟\n\n### 2. استراتيجية البيانات (5 دقائق)\n- ما البيانات الموجودة؟ الحجم، الحداثة، الجودة؟\n- كيف نتعامل مع التسميات المفقودة؟\n- قيود الخصوصية/الامتثال؟\n\n### 3. هندسة الميزات (10 دقائق)\n- الميزات الخام المتاحة\n- الميزات المحسوبة/المجمّعة\n- ميزات التضمين (نص، صور)\n\n### 4. النمذجة (10 دقائق)\n- خط الأساس (قائم على القواعد)\n- نهج ML (gradient boosting / NN)\n- مقياس التقييم ولماذا\n\n### 5. التقديم (10 دقائق)\n- على دفعات أم في الوقت الفعلي؟\n- متطلبات الكمون\n- تصميم مخزن الميزات\n\n### 6. المراقبة (5 دقائق)\n- انجراف البيانات، انجراف المفهوم\n- خطة اختبار A/B\n- مُطلِق إعادة التدريب"},
  "nlp-text-preprocessing-complete-guide": {"en":"## When to Use What\n\n| Task | Preprocessing |\n|------|---------------|\n| BERT/Transformers | Use their tokenizer ONLY |\n| TF-IDF + classical ML | Lowercase, remove stopwords, lemmatize |\n| FastText | Minimal (it handles subwords) |\n| Arabic | Normalize alef/hamza, remove diacritics |\n| French | Handle accents, elisions (l', d') |\n\n## Universal Pipeline\n\n```python\nimport re\nfrom nltk.tokenize import word_tokenize\nfrom nltk.stem import WordNetLemmatizer\n\ndef preprocess(text, language='english'):\n    text = text.lower().strip()\n    text = re.sub(r'http\\S+', '', text)  # Remove URLs\n    text = re.sub(r'[^a-zA-Z0-9\\s]', '', text)  # Remove special chars\n    tokens = word_tokenize(text)\n    lemmatizer = WordNetLemmatizer()\n    tokens = [lemmatizer.lemmatize(t) for t in tokens if len(t) > 2]\n    return ' '.join(tokens)\n```\n\n## For BERT: Trust the Tokenizer\n\nBERT's WordPiece handles casing, subwords, and special tokens. Don't apply custom preprocessing — it will hurt performance.","fr":"## Quand utiliser quoi\n\n| Tâche | Prétraitement |\n|------|---------------|\n| BERT/Transformers | Utilisez UNIQUEMENT leur tokenizer |\n| TF-IDF + ML classique | Minuscules, retirer les stopwords, lemmatiser |\n| FastText | Minimal (il gère les sous-mots) |\n| Arabe | Normaliser alef/hamza, retirer les diacritiques |\n| Français | Gérer les accents, les élisions (l', d') |\n\n## Pipeline universel\n\n```python\nimport re\nfrom nltk.tokenize import word_tokenize\nfrom nltk.stem import WordNetLemmatizer\n\ndef preprocess(text, language='english'):\n    text = text.lower().strip()\n    text = re.sub(r'http\\S+', '', text)  # Remove URLs\n    text = re.sub(r'[^a-zA-Z0-9\\s]', '', text)  # Remove special chars\n    tokens = word_tokenize(text)\n    lemmatizer = WordNetLemmatizer()\n    tokens = [lemmatizer.lemmatize(t) for t in tokens if len(t) > 2]\n    return ' '.join(tokens)\n```\n\n## Pour BERT : faites confiance au tokenizer\n\nLe WordPiece de BERT gère la casse, les sous-mots et les tokens spéciaux. N'appliquez pas de prétraitement personnalisé — cela nuirait aux performances.","ar":"## متى تستخدم ماذا\n\n| المهمة | المعالجة المسبقة |\n|------|---------------|\n| BERT/Transformers | استخدم مُقطّعها فقط |\n| TF-IDF + ML الكلاسيكي | تحويل لأحرف صغيرة، إزالة الكلمات الشائعة، الليمة |\n| FastText | الحد الأدنى (يتعامل مع الكلمات الفرعية) |\n| العربية | تطبيع الألف/الهمزة، إزالة التشكيل |\n| الفرنسية | معالجة اللكنات والإدغامات (l', d') |\n\n## خط أنابيب عام\n\n```python\nimport re\nfrom nltk.tokenize import word_tokenize\nfrom nltk.stem import WordNetLemmatizer\n\ndef preprocess(text, language='english'):\n    text = text.lower().strip()\n    text = re.sub(r'http\\S+', '', text)  # Remove URLs\n    text = re.sub(r'[^a-zA-Z0-9\\s]', '', text)  # Remove special chars\n    tokens = word_tokenize(text)\n    lemmatizer = WordNetLemmatizer()\n    tokens = [lemmatizer.lemmatize(t) for t in tokens if len(t) > 2]\n    return ' '.join(tokens)\n```\n\n## لـ BERT: ثِق بالمُقطّع\n\nيتعامل WordPiece في BERT مع حالة الأحرف والكلمات الفرعية والرموز الخاصة. لا تطبّق معالجة مسبقة مخصصة — فذلك يضر بالأداء."},
  "recommendation-system-collaborative-filtering": {"en":"## Collaborative Filtering\n\n```python\nfrom scipy.sparse.linalg import svds\nimport numpy as np\n\n# SVD-based matrix factorization\nU, sigma, Vt = svds(R, k=50)  # R = user-item matrix\npredictions = np.dot(np.dot(U, np.diag(sigma)), Vt)\n```\n\n## Neural CF with PyTorch\n\n```python\nclass NeuralCF(nn.Module):\n    def __init__(self, n_users, n_items, embed_dim=64):\n        super().__init__()\n        self.user_embed = nn.Embedding(n_users, embed_dim)\n        self.item_embed = nn.Embedding(n_items, embed_dim)\n        self.mlp = nn.Sequential(\n            nn.Linear(embed_dim*2, 128), nn.ReLU(),\n            nn.Linear(128, 64), nn.ReLU(),\n            nn.Linear(64, 1), nn.Sigmoid()\n        )\n    \n    def forward(self, user_ids, item_ids):\n        u = self.user_embed(user_ids)\n        i = self.item_embed(item_ids)\n        return self.mlp(torch.cat([u, i], dim=-1))\n```\n\n## Evaluation Metrics\n\n- **NDCG@K**: Normalized Discounted Cumulative Gain\n- **HR@K**: Hit Rate at K\n- **MRR**: Mean Reciprocal Rank","fr":"## Filtrage collaboratif\n\n```python\nfrom scipy.sparse.linalg import svds\nimport numpy as np\n\n# SVD-based matrix factorization\nU, sigma, Vt = svds(R, k=50)  # R = user-item matrix\npredictions = np.dot(np.dot(U, np.diag(sigma)), Vt)\n```\n\n## Neural CF avec PyTorch\n\n```python\nclass NeuralCF(nn.Module):\n    def __init__(self, n_users, n_items, embed_dim=64):\n        super().__init__()\n        self.user_embed = nn.Embedding(n_users, embed_dim)\n        self.item_embed = nn.Embedding(n_items, embed_dim)\n        self.mlp = nn.Sequential(\n            nn.Linear(embed_dim*2, 128), nn.ReLU(),\n            nn.Linear(128, 64), nn.ReLU(),\n            nn.Linear(64, 1), nn.Sigmoid()\n        )\n    \n    def forward(self, user_ids, item_ids):\n        u = self.user_embed(user_ids)\n        i = self.item_embed(item_ids)\n        return self.mlp(torch.cat([u, i], dim=-1))\n```\n\n## Métriques d'évaluation\n\n- **NDCG@K** : Normalized Discounted Cumulative Gain\n- **HR@K** : Hit Rate à K\n- **MRR** : Mean Reciprocal Rank","ar":"## الفلترة التعاونية\n\n```python\nfrom scipy.sparse.linalg import svds\nimport numpy as np\n\n# SVD-based matrix factorization\nU, sigma, Vt = svds(R, k=50)  # R = user-item matrix\npredictions = np.dot(np.dot(U, np.diag(sigma)), Vt)\n```\n\n## Neural CF باستخدام PyTorch\n\n```python\nclass NeuralCF(nn.Module):\n    def __init__(self, n_users, n_items, embed_dim=64):\n        super().__init__()\n        self.user_embed = nn.Embedding(n_users, embed_dim)\n        self.item_embed = nn.Embedding(n_items, embed_dim)\n        self.mlp = nn.Sequential(\n            nn.Linear(embed_dim*2, 128), nn.ReLU(),\n            nn.Linear(128, 64), nn.ReLU(),\n            nn.Linear(64, 1), nn.Sigmoid()\n        )\n    \n    def forward(self, user_ids, item_ids):\n        u = self.user_embed(user_ids)\n        i = self.item_embed(item_ids)\n        return self.mlp(torch.cat([u, i], dim=-1))\n```\n\n## مقاييس التقييم\n\n- **NDCG@K**: المكسب التراكمي المخفّض المُطبّع\n- **HR@K**: معدل الإصابة عند K\n- **MRR**: متوسط الرتبة العكسية"},
  "speech-to-text-whisper-production": {"en":"## Model Selection\n\n| Model | Params | WER (EN) | Speed |\n|-------|--------|----------|-------|\n| tiny | 39M | 14% | 32x |\n| base | 74M | 10% | 16x |\n| small | 244M | 7% | 6x |\n| medium | 769M | 5% | 2x |\n| large-v3 | 1.5B | 3% | 1x |\n\nFor production with latency constraints: `small` is the sweet spot.\n\n## FastAPI Deployment\n\n```python\nimport whisper\nfrom fastapi import FastAPI, UploadFile\n\napp = FastAPI()\nmodel = whisper.load_model('small').to('cuda')\n\n@app.post('/transcribe')\nasync def transcribe(audio: UploadFile, language: str = 'ar'):\n    audio_bytes = await audio.read()\n    result = model.transcribe(audio_bytes, language=language, fp16=True)\n    return {'text': result['text'], 'language': result['language']}\n```\n\n## Darija Fine-Tuning Dataset\n\nUsed Mozilla Common Voice Arabic + scraped Moroccan radio recordings. Fine-tuning for 3 epochs reduces WER from 32% to 18% on Darija.","fr":"## Choix du modèle\n\n| Modèle | Params | WER (EN) | Vitesse |\n|-------|--------|----------|-------|\n| tiny | 39M | 14% | 32x |\n| base | 74M | 10% | 16x |\n| small | 244M | 7% | 6x |\n| medium | 769M | 5% | 2x |\n| large-v3 | 1.5B | 3% | 1x |\n\nPour la production avec contraintes de latence : `small` est le meilleur compromis.\n\n## Déploiement FastAPI\n\n```python\nimport whisper\nfrom fastapi import FastAPI, UploadFile\n\napp = FastAPI()\nmodel = whisper.load_model('small').to('cuda')\n\n@app.post('/transcribe')\nasync def transcribe(audio: UploadFile, language: str = 'ar'):\n    audio_bytes = await audio.read()\n    result = model.transcribe(audio_bytes, language=language, fp16=True)\n    return {'text': result['text'], 'language': result['language']}\n```\n\n## Dataset de fine-tuning Darija\n\nJ'ai utilisé Mozilla Common Voice Arabic + des enregistrements radio marocains scrapés. Un fine-tuning sur 3 epochs réduit le WER de 32% à 18% sur le darija.","ar":"## اختيار النموذج\n\n| النموذج | المعاملات | WER (EN) | السرعة |\n|-------|--------|----------|-------|\n| tiny | 39M | 14% | 32x |\n| base | 74M | 10% | 16x |\n| small | 244M | 7% | 6x |\n| medium | 769M | 5% | 2x |\n| large-v3 | 1.5B | 3% | 1x |\n\nللإنتاج مع قيود الكمون: `small` هو الخيار الأمثل.\n\n## النشر باستخدام FastAPI\n\n```python\nimport whisper\nfrom fastapi import FastAPI, UploadFile\n\napp = FastAPI()\nmodel = whisper.load_model('small').to('cuda')\n\n@app.post('/transcribe')\nasync def transcribe(audio: UploadFile, language: str = 'ar'):\n    audio_bytes = await audio.read()\n    result = model.transcribe(audio_bytes, language=language, fp16=True)\n    return {'text': result['text'], 'language': result['language']}\n```\n\n## مجموعة بيانات الضبط الدقيق للدارجة\n\nاستخدمت Mozilla Common Voice Arabic + تسجيلات إذاعية مغربية مستخرجة. الضبط الدقيق على 3 حقب يخفّض WER من 32% إلى 18% على الدارجة."},
  "model-compression-quantization-pruning": {"en":"## Technique Comparison\n\n| Method | Size Reduction | Accuracy Drop | Effort |\n|--------|---------------|---------------|--------|\n| INT8 Quantization | 4x | ~1% | Low |\n| FP16 | 2x | <0.1% | Very Low |\n| Pruning (30%) | 1.4x | ~2% | Medium |\n| Distillation | 5-10x | 3-5% | High |\n\n## Post-Training Quantization (Easiest)\n\n```python\nimport torch\n\n# Dynamic quantization (CPU inference)\nmodel_int8 = torch.quantization.quantize_dynamic(\n    model,\n    {nn.Linear, nn.LSTM},\n    dtype=torch.qint8\n)\n# Result: 2-4x smaller, 2x faster on CPU\n```\n\n## Knowledge Distillation\n\n```python\n# Student learns from teacher's soft probabilities\nteacher_logits = teacher(x).detach()\nstudent_logits = student(x)\n\nkd_loss = nn.KLDivLoss()(\n    F.log_softmax(student_logits/T, dim=-1),\n    F.softmax(teacher_logits/T, dim=-1)\n) * T**2\n```","fr":"## Comparaison des techniques\n\n| Méthode | Réduction de taille | Perte de précision | Effort |\n|--------|---------------|---------------|--------|\n| Quantification INT8 | 4x | ~1% | Faible |\n| FP16 | 2x | <0.1% | Très faible |\n| Élagage (30%) | 1.4x | ~2% | Moyen |\n| Distillation | 5-10x | 3-5% | Élevé |\n\n## Quantification post-entraînement (la plus simple)\n\n```python\nimport torch\n\n# Dynamic quantization (CPU inference)\nmodel_int8 = torch.quantization.quantize_dynamic(\n    model,\n    {nn.Linear, nn.LSTM},\n    dtype=torch.qint8\n)\n# Result: 2-4x smaller, 2x faster on CPU\n```\n\n## Distillation de connaissances\n\n```python\n# Student learns from teacher's soft probabilities\nteacher_logits = teacher(x).detach()\nstudent_logits = student(x)\n\nkd_loss = nn.KLDivLoss()(\n    F.log_softmax(student_logits/T, dim=-1),\n    F.softmax(teacher_logits/T, dim=-1)\n) * T**2\n```","ar":"## مقارنة التقنيات\n\n| الطريقة | تقليص الحجم | انخفاض الدقة | الجهد |\n|--------|---------------|---------------|--------|\n| التكميم INT8 | 4x | ~1% | منخفض |\n| FP16 | 2x | <0.1% | منخفض جداً |\n| التقليم (30%) | 1.4x | ~2% | متوسط |\n| التقطير | 5-10x | 3-5% | مرتفع |\n\n## التكميم بعد التدريب (الأسهل)\n\n```python\nimport torch\n\n# Dynamic quantization (CPU inference)\nmodel_int8 = torch.quantization.quantize_dynamic(\n    model,\n    {nn.Linear, nn.LSTM},\n    dtype=torch.qint8\n)\n# Result: 2-4x smaller, 2x faster on CPU\n```\n\n## تقطير المعرفة\n\n```python\n# Student learns from teacher's soft probabilities\nteacher_logits = teacher(x).detach()\nstudent_logits = student(x)\n\nkd_loss = nn.KLDivLoss()(\n    F.log_softmax(student_logits/T, dim=-1),\n    F.softmax(teacher_logits/T, dim=-1)\n) * T**2\n```"},
};

/** Returns the localized markdown body for a slug, falling back to English. */
export function getBlogBody(slug: string, locale: string): string {
  const b = blogBodies[slug];
  if (!b) return "";
  if (locale === "fr") return b.fr || b.en;
  if (locale === "ar") return b.ar || b.en;
  return b.en;
}

/** True only when a genuine translation exists for the locale (not the EN fallback). */
export function hasLocalizedBody(slug: string, locale: string): boolean {
  const b = blogBodies[slug];
  if (!b) return false;
  if (locale === "fr") return b.fr !== b.en;
  if (locale === "ar") return b.ar !== b.en;
  return true;
}
